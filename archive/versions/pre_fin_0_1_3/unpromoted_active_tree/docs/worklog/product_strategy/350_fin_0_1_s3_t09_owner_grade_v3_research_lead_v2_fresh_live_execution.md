# FIN 0.1 S3-T09 Research Lead v2 fresh live execution

The issued Specialist-v5 plus Lead-v2 admission was consumed exactly once after Project OS scoped and runner zero-call preflights passed. The shell wrapper timed out at 14 seconds, but the original runner process remained alive; read-only polling observed it transition from `running/running/running` to `failed/failed/failed`. No second execute command was issued.

All nine Specialist calls and the Research Lead call returned `finish_reason=stop`. Lead-v2 completed at 1,094 of 1,800 tokens, so the historical truncation did not recur. Its four-member provider object passed v2 shape, `3/3/4` cardinality, text, authority, 3,424-byte Provider, and 4,437-byte assembled limits.

The first failure was the subsequent historical canonical check: `s3_owner_grade_lead_fact_presence_mismatch`, safely persisted as `s3_bounded_research_lead_v2_assembly_canonical_validation_failed`. Restricted structural replay found that the canonical rule applies the total three-Cell Fact count to every conflict. It therefore rejects conflict-local `no_facts_present` rows involving only zero-Fact claims/Cells whenever an unrelated Cell has any Fact. Lead-v2 does not yet close the intended fact-presence scope.

Terminal truth is consistent: WorkUnit/Attempt/Run failed, Artifact=0, event=7, orphan=false. Usage was 10 calls, 39,443 input tokens, 5,999 output tokens, 45,442 total tokens, USD 0.02154861, and zero retry/fallback/rerun. Ten final assistant outputs remain persisted in restricted content-addressed storage and were read back without writing their text into tracked files.

RC-P36-040's truncation/capacity repair now has live proof, but a new owned RC-P36-041 fact-presence scope mismatch blocks the chain before Writer and Verifier. RC-P36-037 and T09 remain blocked. The next item is only the separately authorized zero-call root-cause decision; no patch or rerun is authorized.

Closeout verification passed 46 focused current/historical contract tests, including the exact admission, restricted capture replay, Specialist-v5, Lead-v2, and prior-result progression contracts. The scoped Project OS preflight also passed with zero open full-chain blockers for this closeout scope. These checks made no additional model, Provider, or network calls.
