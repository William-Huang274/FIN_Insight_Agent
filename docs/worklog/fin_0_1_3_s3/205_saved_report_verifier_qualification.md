# 205：冻结候选的独立核验与局部修订

2026-09-10；FIN 0.1.3 / S3 / 205，不新增产品版本。

用户授权先看既有核验的实际效果。本切片选205上一轮MSFT恢复候选和MU新方法候选，原件不可变。复用生产 `build_case_output_agent` 的 Verifier、Writer、精确编辑和来源回读，不整篇重跑，不向核验者注入人工预设答案，不再扩写六方法。

执行前预期：MSFT检查分母与结论对象是否一致；MU检查数字同步增长与因果/盈利质量之间的距离。正确金额、期间、图表点值应保留；仅属未核实的原因或未来关注项不应自动升级为阻塞。此处预期仅供人工对照，不进入模型上下文。

最小工程：让既有只读来源投影接收从成功原生工具结果恢复的来源，而不虚构workpaper。采用既有SQLite FTS5、LangChain原生工具循环和checkpoint，不建立新工作流/语义规则引擎。

每题先独立核验，成立的问题交既有Writer局部修订，再独立复核。每阶段最多4模型/10工具调用，每请求最多6000输出tokens、100000输入字符、240秒、一次传输，无自动重试。各阶段单独落TokenBudgetBasis与原始输出；异常/用量未知停止该切片，不续费整题重跑。两题总上限24模型调用，按实际提交提前结束。核验未完成或存在重大错误不算质量通过。

## 实测结果

证据：`D:/temp/fin205-saved-report-review-a1/`，汇总`assessment.json`。每阶段独立目录、manifest输入哈希、SQLite checkpoint、公开事件、私有原始响应/推理；原件哈希均保持一致。四次阶段尝试共10次真实模型调用、179,419已知tokens、新未知0，无自动重试。两份开发候选不是盲测，不代表整体报告错误率。

| 阶段 | 模型调用 / tokens | 实际结果 |
|---|---:|---|
| MSFT 初审 | 3 / 63,119 | 成功交卷；8次来源回读+目录，0重大/2 advisory/0未决。但漏掉CFO/净利润被解释为“经营利润的现金兑现”的对象混用；不能按模型自报授予质量通过。 |
| MSFT 作者修订 | 3 / 50,772 | 原生submit_report_edits提交3处局部编辑，补比较期来源与范围说明。离线精确回放完全一致，原表及完整图表JSON不变；漏检的错误仍保留。 |
| MSFT 修后复核 | 2 / 28,388 | 未提交；第二轮再次批量读6来源，累计请求11工具超过10上限。首轮还调用未挂载的get_research_method，这是本次qualification入口遗漏，不冒充生产runtime缺陷。 |
| MU 初审 | 2 / 37,140 | 未提交；第二次响应到6000输出tokens上限，其中reasoning_tokens=4937，submit_report_review参数JSON被截断。保留原响应，不修补JSON冒充有效核验，不让作者基于半截审查修改。 |

MU未提交参数已识别“本地缺少现金余额/季度数据”与实际目录相冲突；但其已经写出的总结认为结论无因果升级，仍未挑战“非纯账面”和周期回升原因等问题。**这只能算失败响应中的诊断线索，不能算完整审查发现或修订通过**。MSFT则可明确定位：模型实际读到了指标定义，且自己的推理文本讨论了错误句子，却认为附上营运资本限制即可接受。说明这次具体漏检不由来源丢失、解析失败或没读定义解释；它接受了不成立的金融含义映射。原始推理只留私有审计，对外描述判断和证据。

本轮未改变六方法，也未换模型/推理强度。实际provider为api.deepseek.com、别名deepseek-flash、thinking enabled/low，不据别名推定底层v4.1。未获得此次别名账单对照，不给出伪精确费用。延续205既有汇总口径：569模型尝试、17,413,041已知tokens、历史未知2；不是569个独立case。

## 工程与验证

- 既有只读来源投影新增`from_observed_sources`，接收宿主从成功原生工具恢复的记录，检查原始身份并深拷贝；无伪造workpaper、无新证据准入或新工作流。
- 有界脚本复用生产Verifier/Writer/精确编辑、原生LangChain循环和SQLite checkpoint。发现方法工具遗漏后已补上既有方法读取入口，并离线验证catalog/role读取；**没有付费重跑验证这个修复后的效果**。
- 32 passed / 1 skipped，覆盖来源身份/副本隔离、真实编译图读取及局部提交、既有审查与图表路径。初跑30通过/1跳过/1失败系旧Lead图表夹具使用伪单位`source units`；按已存在严格单位规则改用实际来源单位，再全组通过。跳过仍非通过。
- Project OS预检通过，属于配置契约检查而非产品验收。精确编辑离线回放与冻结输入哈希检查通过。
- 无用户报告替换、无前端变更、无服务部署、无全量研究重跑；后台无遗留付费调用。
- 工程与测试提交`90e37a54`，工作分支`codex/fin013-conversation-and-retrieval`；本轮8个修改候选文件密钥扫描无命中，差异空白检查通过，原始私有响应/数据库留外部证据目录。

## 判断与后续边界

产品增量：本轮未发布新的用户报告或UI。工程增量：原有核验/编辑能处理无workpaper的宿主来源视图；测试入口遗漏修复。研究证据：当前核验能查数与部分来源边界，但此次**没有形成一份已通过完整复核的修后报告**。记录与台账只承担人类记忆，不作为运行控制面。

不建议把当前Verifier的“无重大问题”直接设为自动发布门槛。下一项应在同一来源和问题上限定检查“具体句子的指标对象、证据支持到哪、结论多说了哪一层”，结合正确句控制项检查误报；先使已有审查保留紧凑发现、提交额度与实际变更范围，避免重新核算整篇后没预算交卷。采用现有结构化报告发现/精确编辑即可，不写公司/NLP关键词规则，不因为两例就升级模型或全面增长六skill。是否有效仍需新的、有明确假设的对照，不能把本次负结果写成改善。
