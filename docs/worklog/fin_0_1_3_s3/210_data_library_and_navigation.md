# 210 结项前资料覆盖与界面整理

Owner 2026-09-11 新增：补齐案例公司的 SEC 原文与 SQL 覆盖；可视化资料目录与筛选财务数据；研究地图预览；研究 / 资料与数据两入口。

基线 a45375df，FIN0.1.3 / S3 不变。原报告与失败运行不回写。

发现：研究原文仍使用旧固定 S1 目录，财务 mart 已扩至 DELL/HPE/MSFT/MU/NVDA 2274 行；跨公司原文和 SQL 覆盖不一致。公共资料不得通过无权限读取其他用户上传材料来补齐。

实施：复用 SEC submissions、现有 sec2md/trafilatura 结构化解析、原有 retrieval node 投影、StructuredLocalKnowledgeReader、BM25/Qwen 缓存；生成版本化公共资料快照并接入研究与普通对话。BFF 参数化只读查询 SQL；React 两入口与响应式卡片。无需新检索引擎或工作流。

验收：实际五公司/期间目录，原文读取与定位，SQL 条件与分页，前端桌面/移动布局，历史报告可读。先零模型验证；不整篇重跑已完结研究。新向量准备如发生使用既有 CachedRetrieval 的 TokenBudgetBasis，记录实际用量；不得将未准备的向量称为已使用。

## 实现与真实证据

- 新增40份官方 SEC 原文：DELL/HPE/MSFT/MU/NVDA 各3份10-K、1份10-Q、2份业绩相关8-K及2份新闻稿。MSFT含2024/2025/2026年6月末年度；HPE/MU含2023–2025年度；DELL/NVDA含2024–2026年结束年度。并非所有期间的全部申报文件。
- 与原20份文档合并为60份公共资料、7510个结构化节点、11015个向量片段。保留原SEC响应、文档身份、原始链接、来源哈希、章节和表格。数据在 `D:/temp/fin210/source-a3`，快照SHA256 `6f377ecf4626e74c04673bff909c207a1c6b53b9c0e558cbb815e182cd33bdf5`。
- 沿用现有五公司2274行财务mart；覆盖2022起的已采集期间、12类源指标；本轮没有虚增SQL行数。现有37个派生指标继续由研究工具执行，浏览入口列示的是来源披露记录。公司/指标中英文关键词、财年/期间、披露截止日、分页均支持；原单位、原期间及各披露版本保留。
- 两个主入口：研究工作台 / 资料与数据。后者只保留公司资料库和财务数据导航。资料卡片有封面式摘要、公司、期间、类别、发布日期、章节数，点击打开可滚动正文；空覆盖可把当前范围带入允许公开外源补充的研究准备页，不自动付费运行或公开私人附件。
- 研究地图改为全宽章节卡片网格，展示正文摘录，进入章节/判断/原文的旧链接不变。
- 研究reader使用新公共快照（存在时）；普通对话增加 `read_company_library`，并接通来源凭证、回读、交接和上下文分区。公共文件不升格为已核验事实；旧报告和失败快照不改写。

## 验证

- `tests/test_data_library.py` 11通过：真实工具到交接凭证、过滤、时点、分页、参数化SQL、防任意路径、启用身份后的未登录拒绝。与source_hybrid同批13通过。
- 相邻 conversation_tools/handoff/model_context/data_composition/task_attachments：36通过、1个私有材料依赖跳过。
- 原导航/研究图/修订交互：9个公共Playwright检查通过。
- 真实公共原件经产品API与已有Agent reader：15份年报均能检索和完整回读现金流相关原文；5家公司FY2025 CFO均可查，见 `D:/temp/fin210/product-probe-a1` / `product-probe-a2`。
- 真实API回执驱动浏览器，1440/2160/390三宽度；正文滚动、关闭、数据切换、章节多列布局、无整页横向溢出通过。最终截图 `D:/temp/fin210/ui-a4`。数据API在这些浏览器检查中以实际TestClient回执拦截，不冒充线上已部署。
- 新镜像构建完成；无网络/只读临时容器的真实普通对话工具列出60份资料。镜像tag已构建，不等于旧运行容器已经重建。
- TS与Vite构建通过。仍有原有大JS bundle提示，不是构建失败。

## Qwen缓存与费用

完整新快照准备：1091次embedding请求、3561210已知tokens；5公司定向混合检索追加1次共享query embedding与5次rerank、75343tokens。合计1097次检索API请求、3636553已知tokens；0次DeepSeek/生成模型调用。按2026-09-09记录单价0.5元/百万tokens估算1.8182765元；不等同实际账单或免费额度抵扣。

全快照再次准备0次API请求，5条查询逐条重放也均0次API请求；5/5使用 `qwen_dense_bm25_rerank`。这是已接通与复用证据，不是新增金融答案准确率或统计召回率声明。缓存位于既有部署 `working-memory/source-rag`，不创建新检索引擎。

初次解析因宿主缺sec2md失败，原下载件保留；补齐锁定可选依赖0.1.23并将依赖检查提前，再复用原件。未通过缩弱解析合同掩盖失败。

## 当前部署阻塞

自动审批拒绝包含资料发布和18795进程重载的命令，仅返回 `blocked by policy`。命令未执行，公共快照尚未发布到运行设置目录，18165运行进程未加载新reader；不能把已构建静态前端或TestClient资格当成完整部署。

已准备 `scripts/deployment/publish_data_library.ps1`：显式源/目标、保留快照/旧文件、检查研究和普通对话无busy、核验18795进程归属、使用已有compose入口更新native服务、后台重载BFF、检查实际数据API。脚本语法通过，未由Codex换包装执行拒绝动作。Owner已收到手动执行命令；18796 sandbox不在操作范围内。

FIN0.1.3结项仍暂停；本包属于产品实现+数据/检索资格增量，待部署回读后才能称正式本机可用。

代码与定向资格提交：`8102e95d`，分支 `codex/fin013-conversation-and-retrieval`。数据原件、向量缓存和截图留在独立工件目录，不进入Git；新增内容密钥扫描与diff检查通过。

## 重启后操作者入口修复

Owner重启电脑后实际执行：第一次在system32使用相对路径未找到脚本；切换仓库后第21行请求18795失败。只读核查确认Docker引擎在线，但当前工作台三容器及18165/18795/18796/18806均停止；原件和配置仍存在，发布前已退出，无资料改写。

根因是本脚本仅支持在线重载，把在线BFF当作启动前置条件。现允许冷启动：无BFF时检查项目native容器；若native在线则直接只读查询busy线程，不能将BFF离线当作没有运行任务；全停时无需停止任何进程。在线路径保留进程归属及研究/普通对话busy守卫，异常通信仍停止发布。Python依赖预检前置。

`tests/test_data_library_deployment.py` 九种模拟分支通过；本机提取并单独执行只读preflight确认cold，解释器依赖通过。未执行发布/重载操作，不绕过此前审批拒绝；交付绝对路径命令供Owner重试。此修复为启动工程增量，0模型/0检索费用，部署仍待实际完成回读。
