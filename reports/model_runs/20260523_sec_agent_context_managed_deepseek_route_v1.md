# SEC Agent Context-Managed DeepSeek Route Eval v1

## Run Metadata
- Date: 2026-05-23
- Run ID: `20260523_sec_agent_context_managed_deepseek_route_v1`
- Type: tool-call controller route evaluation
- Model: DeepSeek `deepseek-v4-pro`
- Mode: route-only; no graph/DAG execution; no GPU workload
- Entry point: `scripts/evaluate_sec_agent_context_managed_tool_controller.py`

## Purpose
- Validate the controller path after replacing hand-built eval `runtime_context` with `ContextManager.build_controller_context()`.
- The route input now comes from persisted fixture `session_state.json` files via `SecAgentContextManager`, including lossless fields for identity, active answer, active scope, artifact state, resume cursor, and source policy.

## Inputs
- Eval set: `eval_sets/sec_agent_multiturn_noncontiguous_followup_eval_v1.json`
- Scenario count: `6`
- Turn count: `23`
- Fixture sessions generated by the evaluator:
  - `s_tool_002`
  - `s_tool_003`
  - `s_tool_004_a`
  - `s_tool_004_b`
  - `s_tool_005`

## Results
- Local heuristic context-managed route:
  - Output: `reports/quality/local_context_managed_tool_controller_route_heuristic_v1.json`
  - Result: tool pass `23/23`, arg pass `23/23`, snapshot pass `23/23`, `all_pass=true`.
- Cloud heuristic context-managed route:
  - Output: `reports/quality/cloud_context_managed_tool_controller_route_heuristic_v1.json`
  - Result: tool pass `23/23`, arg pass `23/23`, snapshot pass `23/23`, `all_pass=true`.
- Cloud DeepSeek context-managed route:
  - Output: `reports/quality/cloud_context_managed_tool_controller_route_deepseek_v1.json`
  - Result: tool pass `23/23`, arg pass `23/23`, snapshot pass `23/23`, `failure_count=0`, `all_pass=true`.

## Interpretation
- DeepSeek can route the widened non-contiguous follow-up suite when the controller receives context from persisted session state through `ContextManager`.
- This closes the previous gap where route eval depended on hand-built per-turn `runtime_context`.
- This run does not prove tool dispatch state mutation yet; the next increment should route from a ContextManager snapshot, dispatch the selected harness tool, then call `ContextManager.apply_tool_result()`.

## Safety
- No API key or cloud password is stored in this ledger.
- The run is route-only and does not execute SEC retrieval, BGE reranking, synthesis, or deterministic gates.
