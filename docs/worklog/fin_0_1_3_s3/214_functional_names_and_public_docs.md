# 功能命名与公开文档

2026-09-13 · FIN 0.1.3 · 分支 `codex/functional-names-public-docs`。

用户要求审查 Dell 命名代码：未引用的案例特例归档，现用实现按功能命名；公开文档改为面向读者的产品说明。基于 PR #8 合并提交 `ec709194bd7cf8aabf33f1e1158d3b67cac456a7` 开始，工作区原先干净。

## 实现与审查

22 个 Dell 命名运行模块全部存在现用调用，包括会话、数据组合、身份、审阅、专家、来源编译和执行检查。它们并非未引用的废弃实验。逐项职责与调用记录见[命名清单](../../architecture/repository/module_naming_migration.json)。当前仍受参考案例数据/方法配置约束；改名不宣称任意行业泛化。

部署实现从 `dell_report_workbench.py` 合入 `research_workbench.py`，原转发层退出。22 个模块、37 个测试、两份 SQL 源文件及部署目录同步重命名，更新静态导入、动态工具调用、CLI、Docker/Compose 和注册入口；共 81 个原路径变更。所有原件由上述基准提交保留。

图 ID、现有 MCP 工具 ID、环境变量、凭据派生标识、数据/权限 JSON 合同保持不变。SQL 文件内容、旧安装脚本和数据库 catalog fingerprint 字节不变；容器中的旧 SQL 挂载名服务于安装器兼容。

公开导航改为产品导览、安装、架构、评测、版本说明和路线图。README 保留真实功能截图，移出内部执行汇报；个人简历指导页改为工程证据导航。部署与沙箱说明使用通用路径示例，保留必要的认证、停旧进程与恢复要求。原始测量、失败记录和历史工作日志不改写为成功。

## 验证记录

- 静态活动依赖 A1/A2 均通过。SQL 原文与基准 Git blob 精确相同；其他数据/权限 JSON 无改动。
- 完整 pytest A1：25 failed / 1432 passed / 134 skipped。SQL 加载器与挂载路径有遗漏；自动符号改名还把既有 case-method MCP 工具 ID 改成通用方法工具名，造成注册冲突。保持已发布协议 ID，仅改内部函数名后修复；没有放宽来源/财务校验。
- 定向 A2：72 passed / 1 failed；旧 SQL 安装脚本摘要校验捕获了容器路径改写。恢复旧脚本原字节及旧容器内挂载名称，源码路径使用新名称。
- 定向 A3/A4：更新多段字符串中的挂载预期，并恢复部署文档必需的身份、停止旧写入者及数据库迁移说明。部署与文档 A5：21 passed。
- 完整 pytest A6、公开文档导航检查及远端 CI 结果在本记录后续追加。原始失败输出保留 `D:/temp/fin214/`，不触发付费模型或真实数据库迁移。

产品增量：无新增金融能力。工程增量：功能命名、去除转发层、兼容性与命名回归检查。研究证据：沿用原评测。文档增量：面向读者重写公开导航与说明。

本地最终验证：完整 pytest A6 **1458 passed / 134 skipped**（282.20 秒），不累加定向组；公开导航可达 25 篇文档，链接缺失 0、所查内部表述 0。现用依赖 A3、源码编译、部署 CLI 帮助、敏感模式扫描和 diff 检查通过。评测文档中的已归档运行命令也已改为当前检查入口与历史版本链接。原始 JSON 评测数据未改，未增加付费模型调用。远端检查和发布以本分支 PR 的匹配提交为准。
