# FIN 0.1.3：交互切片 A，真实报告阅读与来源核对

2026-09-08。Owner在概设/详设和可点击原型交付后要求“继续”，按认可方向推进第一切片A。B追问修订、C研究入口/进度、D整体收口仍需后续实施；不把这轮描述为全部前端改造完成。

## 产品改动

- 移除宽屏强制同时显示长对话和报告的CSS规则。默认报告为主，追问与反馈独立切换；报告态不再占用大段发送框空间。
- 任务头使用真实任务标题、信息时点、当前/历史阅读版；任务说明、上传、资料与分工折叠，未解决研究问题仍保留显式提醒。区分同名任务的简短标识。
- 原报告Markdown标题生成稳定位置锚点与目录，同名标题可分别定位。大屏目录侧列、小屏章节选择器，保留完整正文与图表。
- 来源/审查/运行按明确入口展开；宽屏给来源独立区域，窄屏切为辅助阅读页；关闭或Esc回原引用焦点和报告阅读位置。
- 既有计算接口已返回结构化calculation，前端展示原表达式、结果、完整操作数、单位、期间和核验状态；操作数按来源GET读取，可返回上一级计算。原始记录仍可展开，不由前端重新计算或提升权威。
- 来源加载与错误就地显示，可重复读取；请求代次防止关闭、换任务/版本后旧响应回填。读取旧会话时明确显示加载态，不闪现新研究表单。

## 工程与边界

复用React、react-markdown/remark及现有BFF/native来源、版本、导出和会话合同。目录是既有Markdown AST的薄适配，无新解析平台/状态机/后端服务/依赖。原生镜像与模型节点未变，不修改v4正文、旧绑定或金融质量判定。

引用记录的原始主张可能早于报告修订，明确标注，不能把其措辞当作当前报告的新结论。语义问题仍由原研究审阅处理，不在UI做自然语言纠错。

## 验证

TypeScript与生产构建通过。浏览器A1在1440/1024/390全部发现关闭来源后焦点未恢复：Markdown重渲染使原DOM引用失效；保留原失败截图与上下文，按来源在报告/对话中的位置恢复对应引用，未降低断言。A2三宽度全通过，覆盖重复标题、阅读位置与焦点、裸引用边界、计算操作数回读/返回、gap回执、运行费用、历史版本/差异/来源/导出。另补来源503就地失败与重新读取检查，由最终候选再次执行。

本地证据：`D:/temp/fin-report-reader-a1/`（失败原件），`D:/temp/fin-report-reader-a2/`（修复后），A3/A4各三宽度通过（含来源503→重读、两种已存计算形状）。真实8766会话读取v4并点击现金流来源/计算，使用既有保存结果，0新增模型调用。本轮先交本地体验版，未推送、未合main，未声称公共CI已验证这一候选；不将工程通过当Owner易用性验收。

真实走查另发现：v4的部分旧计算以JSON文本窗口返回，而较新来源有独立calculation字段。复用标准JSON.parse读取已存结构（完整表达式/操作数才展示），不完整窗口保留原文；原生操作数别名按已有记录回读。v4引用21的P01:S048已显示cfo_q2/ni_q2，操作数原文GET成功，返回计算再关闭可回原引用。未擅自补单位或纠正旧计算rationale；未单列的字段明确显示未提供。

交付状态：产品阅读增量与工程实现；研究资格增量0、模型token/费用增量0；文档记录本轮授权、结果与范围。下一步让Owner体验切片A，再继续B/C/D；Hermes仍后置。
