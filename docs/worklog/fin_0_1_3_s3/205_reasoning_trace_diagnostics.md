# 原始推理字段诊断与强度对照（2026-09-10）

## 授权与边界

Owner 要求逐项检查未解决问题的原始推理字段；能证明的小接口错误先修回放，推理本身有问题才做 high/max 对照。沿用 FIN 0.1.3 / S3 / 205；不创建产品版本，不重跑完整 HPE，不改原失败或正式报告。原始推理和输入仅存私有目录，不提交 Git。

## 首个新证据与修复

HPE search-review-a1 第五请求 0c021c5b-3e76-49f8-8f8b-54a4397db6de：当模型识别前一计算的符号错误时，ReviewWorkBudget 在剩余两次调用时已经只提供 record_case_finding/submit_case_review，且执行层也拒绝重新读取/计算。因此此前将未重算归为模型未自纠并不完整：runtime 同时阻断了纠正路径。

修复仍采用 LangChain 原生 middleware；倒数第二次保留原有范围内工具，最后一次才仅收口；模型调用上限仍6、工具上限仍16。22项审查测试通过。真实历史请求零模型回放见 D:/temp/fin206-reasoning-audit-a1/hpe-archived-tool-replay.json：第五次6种工具、最后一次2种；这只证明可达性，不证明金融审查完成。原始失败保留。

## 付费对照预注册 / TokenBudgetBasis

最多6次独立请求：HPE Flash 的 low/high/max 各1；Micron Pro 的 low/high/max 各1。均 thinking enabled、输出上限12000（包括推理）、超时480秒、transport attempts=1、无自动重试、不执行返回工具、不回写用户窗口。每个节点完整 TokenBudgetBasis 位于私有目录 hpe-budget.json / micron-budget.json；输出上限一致，截断/错误停在原样，不能提升为研究通过。

HPE 使用第五次真实消息及工具结果，只替换提前关闭工具的已知 runtime 提示，统一当前6个原生工具schema；三档同输入，16k/keep2请求投影一致。Micron 使用实际失败最终回答前的原始消息，保留全部工具结果和自身推理，无摘要或新答案提示，统一当前问答节点的11个原生工具schema。SDK前实际 messages/tools 哈希保存用于证明档间一致。

历史日志未保存完整 tools schema，因此本次是“相同归档内容、相同当前接口下的强度对照”，不能称旧版本完整原样复现。Micron schema 由正式工具定义在不执行依赖的资格服务器中枚举；没有使用 fixture 返回值作证据。两组都不引入本轮人工发现的金融修正提示。

输入规模：HPE归档序列化约14.6万字符、正文约5.6万字符；Micron约35.8万/正文16.3万字符。字符不是tokens。按既有2026-09-07情景费率估算费用并分开记录缓存，非账单；不自动为高强度截断增加输出额度。官方思考模式文档确认 low/high/max；medium与xhigh映射high，不设虚假的独立medium实验。来源：https://api-docs.deepseek.com/guides/thinking_mode/ 。

验收先看语义，再看格式：HPE需正确区分总额/增量、亏损符号与桥接覆盖、不把未核实内容标完成；Micron需正确比较简单FCF与净利增长，不能凭折旧金额就断言现金流因果，且区分财年数据/过去的指引/研究时点。工具格式正确只算机械结果。每档仅1次，不能估计可靠通过率或直接更改全局默认强度。

## 结果

### 检索覆盖与逐例定位

索引75份私有审计文件、1084条模型响应，其中858条保留非空推理字段；不是逐条人工验收了1084条。重点对齐已知未决错误发生的调用/前后工具结果。兼容原生 request/response 分行及旧 combined-record 两种日志格式，避免把 combined 误判为没保存。索引和响应副本在 D:/temp/fin206-reasoning-audit-a1/，原件不改。

| 问题 | 实际记录与判断 | 本轮处理 |
|---|---|---|
| HPE 公式符号与未纠正 | 第四轮推理理解负数亏损，却发出减去正数437的公式；第五轮识别错误，此时runtime禁用计算器。新证据同时证明参数错误及纠错路径阻断 | 提前关闭工具已修；原请求工具可达性回放；三档真实下一动作及计算器回放 |
| HPE 未决项被遗漏 | scoped-review-a2最后一轮01a2e3ef-9f33-4bff-8e47-dc505edf44be推理承认归属未读完整，最终参数漏未决项；第五/六轮来源工具同样被提前关闭 | 必填completion/未决列表是既有修复；不能追认原提交通过。本轮修第五轮可读，未重跑整篇复核 |
| HPE 自由调度范围收缩 | a5 Lead若干决策没有非空推理字段，公共reason_summary出现Q1可覆盖其他方向的论断 | 能审查公开理由与任务覆盖，不能声称已检查这些调用的私有推理；本次不把它冒充low→high反例 |
| NVIDIA 财年方向 | specialist-aafeca770447-96c0e6d4f3381d21b63e：来源日期及推理的连续期间判断正确；正文却写“前一财年期末即后一财年期初的次日”。下一轮545ea1b6d3265a7cb432只修C4类型并保留错误正文 | 定位到表达/提交与语义复核脱节，不能说SQL日期错或模型完全不懂财年；不写针对这句话的NLP替换规则，不直接改旧报告 |
| Micron CFO/FCF/增长与期间 | writer 289547b4-bb5e-4418-83e9-c39da75c91e2：推理知道CFO、gross/net capex、simple/adjusted FCF不同，但后续把简单FCF增长与净利增长关系说错，并过度解读折旧加回；还沿用已过时的FY26 Q1指引。FY24设备合同付款149M是调节项，不是政府激励 | 完整真实输入三档对照；问题在推理本身及后续表达，不能用schema合法替代判断 |
| MSFT 编号与放弃图表 | specialist-1230101b3e74-100c1e097c73f142b94d正确讨论收入范围但多次JSON无效；counter492080c5-7665-40a7-b587-b7809e26509d承认缩短ID不存在；writer ce1598fa-a264-4316-b946-c40ce67e9dcf为了避免引用错误计划删图 | 指向引用身份/提交负担和模型参数生成；不把它简单归为金融知识不足。不自动猜ID、删图或放宽引用 |
| MSFT 长审查反复自我讨论 | a34350db-80ae-4c50-ab6b-b89e8df78ed0约4.84万字符推理反复权衡要不要提意见、担心引文，最终无发现 | 重复推理确实存在；推理长度不是更高质量证据，不能据此证明全部结论错或压缩造成 |
| Dell 现金下降/现金消耗 | 保留的writer记录中能识别CFO/FCF仍为正及不同季度变化，后续仍存在叙述强度/图表源选择压力（例如bc53c051-69e5-4614-83ac-38ed5b9ef0be） | 不是所有现金表述问题都来自概念不懂；本轮未宣称已定位每条旧句的唯一原因或重新验收Dell |
| 普通问答、个人知识跨窗、若干SQL与RFC调用 | 对应调用thinking disabled，响应无原始推理文本 | 明确不可追溯内部推理，只能检查真实输入、工具结果和最终回答，不能伪造旧推理或归咎low |

### 固定输入真实对照

三档各1次，仅不同reasoning_effort；HPE使用同一Flash别名，Micron同一Pro别名，均12000上限、480秒、无重试。HPE三档SDK消息hash相同110f4f1072…、工具aeff44de69…；Micron消息a7afe8e50d…、工具0218520af6…相同。供应商Micron计费输入分别84320/84399/84412，尽管客户端消息一致，保留原用量差异，不修改成同一个值或猜测后台原因。

| 节点/档位 | 输入/输出tokens | 推理tokens | 秒 | 真实下一动作与本地回放 |
|---|---:|---:|---:|---|
| HPE low | 28733 / 748 | 600 | 5.0 | 回读已存CALC，saved/saved返回1且操作数保留约0.804；没有正式复核提交 |
| HPE high | 28733 / 2980 | 2522 | 14.0 | 正确重算意图，但给来源CALC又加assumption_note，被真实计算器拒绝source_operand_cannot_be_relabelled_assumption |
| HPE max | 28733 / 6044 | 5469 | 28.5 | 一个回读已存CALC运算返回2112；第二个被同类约束拒绝，且把计算差额247/244当成原文literal；完整重算不通过 |
| Micron low | 84320 / 3284 | 3177 | 59.2 | 再搜索来源，未执行新的外部查询；推理仍存在现金质量归因过强/旧指引问题，无最终回答 |
| Micron high | 84399 / 3098 | 2766 | 50.9 | 两个数值比率均通过真实计算器；推理仍用CFO/净利较高支持现金质量高、沿用旧指引；没有正式答案通过 |
| Micron max | 84412 / 1458 | 815 | 26.7 | 两个比率通过，第三个缺来源ID/显式假设被拒；还把整美元NUMFACT与48/2005百万美元literal混合，单位语义不通过 |

六条JSON工具形状都合法，但实际计算器有3个拒绝参数。计算器回放仅复用真实归档成功工具产物及原HPE修订来源；没有从模型推理补造来源，没有执行fixture handlers。执行代码/私有结果见 replay.py、各臂local-tool-replay.json。原低档历史正文未当作本轮低档结果；本轮没有任何完整答案或复核完成。不得从单次next-action试验推算报告通过率。

新增6次真实调用共356942tokens、全部usage已知、场景估算¥2.830836；没有自动重试、截断后补额度或工具后续模型调用。沿205已有同口径累计为510尝试、16071893已知tokens、场景估算¥19.0275293、历史未知仍2。此次价格估算沿用2026-09-07价格及时段模型，不是账户扣款；直接价页当前抓取重定向，未冒称本次核实了新的计费价。

### 本轮决定与未解决问题

1. 工程修复：保留一次收口前纠错窗口，原调用额度不增加；不能说所有runtime错误均修完。
2. 强度结论：不统一切high/max。提高档位没有稳定消除参数错误和金融语义过强，max在两组也产生新的不可执行参数。单样本不构成强度无效的普遍证明。
3. 对照限制：恢复旧会话自身推理是官方工具调用协议要求，保留原错误轨迹，因此是“从故障位置接续”实验，不是无污染、从第一轮起全high/max的实验；后者需要另立小范围预算，不用整HPE尝试代替。
4. 后续应把“原始数值、来源引用、假设/推导、单位、期间”通过现成结构化工具schema分开，使同一字段不承担互斥含义；严格引用/单位意义仍保留。本轮未加财经NLP逐句规则，也没有把模型写错的公式自动改成预期答案。跨字段约束更好表达与财务语义复核仍未解决。
5. 输入冗余仍存在：Micron本次SDK消息正文中user32525字符、工具124890字符、自身历史推理53694字符；HPE对应610/52672/16129。这是带来源、旧目录与多轮推理的历史，不是纯摘要。当前对照未清空历史/偷减证据，也不足以将所有错归于压缩。

### 检查、部署与交付类别

定向审查/对照准备26通过；结构化模型37通过；来源计算6通过4跳过，共69通过4跳过（不重复计数）。来源计算旧测试把带完整PASSAGE、writer_citable=true的对象当搜索preview，和40fd7859已支持的完整知识回读冲突；改用真实navigate_source_nodes生成不可引用preview，失败/伪工具/用户文本仍拒绝。未更改来源准入代码或放宽准入。

产品增量：本轮无新报告、UI或金融验收。工程增量：提前关闭工具修复、已有诊断CLI支持冻结原生schema/max/预算前置检查/SDK输入哈希；既有回归夹具纠正。研究证据：跨case原始推理检查、6次固定输入对照和零模型工具回放。文档增量：本记录及Project OS更新。完整HPE报告、金融语义、全链路稳定性、Hermes整体资格仍开放。

已构建镜像sha256:339b7f9da873d3e7d1bb41b3b271e696591922ac6cf415f98df043a1cd6c15f3，使用既有research_workbench build、fresh-only和原settings，无运行任务变更。无网络/只读root隔离容器检查确认新ReviewWorkBudget已装入镜像，容器随即移除，未启动服务。构建日志D:/temp/fin206-reasoning-audit-a1/build.log。开始检查时本工作台API/PG/Redis容器均Exited（约一小时前停止），没有运行中的模型。此次仅构建含修复镜像，不自动启动可能有未核清排队任务的服务，也不把镜像构建称服务已加载。

源码与诊断提交3c1272be，继续开发分支codex/fin013-conversation-and-retrieval，不合并main或升版本。定向编译通过、JSONL可读；凭据扫描最初宽泛sk-模式误中历史task-/risk-/disk-标识，改用凭据词边界后未发现候选凭据；私有输入/输出仍全部在临时目录，不提交仓库。
