# 195：真实研究依据图首版

2026-09-08，FIN 0.1.3。Owner认可概念B并要求“先改一版出来看看”。在本地codex/fin013-report-reader继续，以f3115452为基线；不推公开草稿分支、不改研究记录。

## 产品增量

- 已有报告默认进入研究图；引用选择器按原54条记录逐条聚焦，原始主张与报告正文身份明确区分。
- 真实引用→来源→保存计算→操作数依据图。画布由React Flow提供缩放/平移/适配；来源只读，引用关系不允许任意改线。多计算引用展开时聚焦一条计算，其他来源仍可选择，避免节点过小。
- 点击来源固定到所选报告的checkpoint读取，兼容原有calculation结构和legacy JSON；操作数通过已有alias回读。算术与金融语义状态分开展示。
- 每条引用可填写独立修订草稿、清空、查看关联范围；切换引用/报告视图保留当前页面草稿。改变任务或报告版本隔离组件状态，刷新清空。
- 草稿明确未连接模型执行。没有分支持久化、自由编辑事实或精确局部重跑。原有报告、目录、来源和历史版本阅读保留。

## 工程增量

采用`@xyflow/react`12.11.6，npm注册表读取MIT和React>=17 peer范围，React19 TypeScript/构建/浏览器集成通过；锁定依赖新增20包。无新后端、图数据库或调度器。系统无npm入口，第一次猜测npm-cli位置失败后，使用已捆绑pnpm dlx npm11.9.0操作原package-lock；未引入pnpm锁或变更项目包管理器。

来源只在需要时GET，版本未定位禁用读取，失败就地重试；请求代次防止切引用/节点/版本后旧响应回填。关系来自现成citation和CALC，没有事后生成模型思维记录。修订草稿为本地意见，不冒充服务端版本冲突安全或已生成候选。

## 验证与研究证据

- TypeScript noEmit、Vite构建通过；最终静态包index-DlDIZl73.js / index-CcvZW5HE.css。原有大包警告仍在，未将本轮称为加载性能优化。
- A1：6浏览器通过。真实屏幕核对发现多计算全展开导致节点缩得太小，改为聚焦单条计算并增大字；A2：1440/1024/390三宽度图交互及原阅读历史回归共6通过（8.2秒）。A1原件保留，不把视觉调整当产品升级。
- 图测试覆盖：来源503/重试、现代/legacy计算、alias与指定checkpoint来源读取、金融语义未验证、分引用草稿隔离、报告往返和0POST/无横向溢出；不声称全仓回归。
- CUA真实Dell v4第21条引用读取P01:S048，显示`cfo_q2 / ni_q2`与原0.5383498669…，原金融语义未验证保留；未重新计算或纠正原结论。最终图与操作数回读结果见后续交付核对。
- 最终CUA核对：单条计算图可读，cfo_q2=2225/ni_q2=4133；点击cfo_q2按既有alias读回Dell FY2027 Q2 SEC EX99.1原文，页面error日志为空。应用已打开为tab6，供Owner体验。实现提交c87dcddb；文档另行本地提交，未推公开分支。
- 本轮0模型、0正式服务写调用，未发起报告修订。Hermes未开始。正式产品方向仍需Owner体验反馈。

## 持续边界

本轮是可用的真实依据图与本地修订草稿，尚不是完整可编辑推导图。缺少显式假设/反证关系、基线绑定的写接口、候选分支与精确局部执行；下一工作包应按Owner体验意见做最小实现，不扩写第二套运行时规划。
