# FIN 0.1 S3-T09 Research Lead-v3 fresh Agent proof decision

The user authorized only the fresh proof decision. No admission was issued or
consumed, and no model, Provider, network, source, external tool, canonical
Run, Artifact, business Case, comparison, or Human Review write occurred.

Two independent invocations of the clone-only double-prepare path produced the
same new WorkUnit, Attempt, ResearchRun, exact input, prepared payload, and
prospective admission digest.

Closeout regression exposed an RC-P36-038 recurrence in a historical Lead-v2
capture test: the test instantiated `CaseService.for_fixture_root` directly on
the target runtime. The migrating store changed the SQLite physical digest from
`764a1f...c8b` to `3a4390...458`. It created no prospective execution object;
canonical execution counts remained `10/10/10/13`, and the logical snapshot and
object-tree digest were unchanged. The test now copies the canonical root to a
disposable directory before service initialization. Two corrected prepares then
held the current database, object-tree, and logical snapshot unchanged. Closure
means the unsafe test method is repaired and the new physical head is frozen;
the earlier bytes were not restored or misreported as read-only.

The prospective proof is frozen as one DeepSeek `deepseek-v4-pro` execution
using Specialist-v5 and Research-Lead-v3 under output-v3. Its envelope is 12
semantic/Provider/network calls, 16800 aggregate maximum output tokens, USD
0.10, one transport attempt per call, no retry, no fallback, no automatic
repair, and no hidden rerun. Final assistant outputs remain under the existing
restricted capture policy.

The hypothesis is narrow: Lead-v3 should retain the live-proven Specialist-v5
and Lead-v2 capacity envelope, remove the owned global-scope false rejections,
and still reject a real conflict-local truth-table mismatch. Any credible
parse, shape, schema, semantic, or length failure must terminalize the future
Run immediately. Success requires the complete six logical nodes and nine
Artifact families; transport-only success is insufficient.

The prospective admission is
`fin01-s3-t09-three-cell-deepseek-owner-grade-v3-specialist-v5-research-lead-v3-exact-admission-r1`
with digest
`f4fede4d86274bf099e801c8ec89ff0773a7cc9f66baa5365cbcf3ac39d3b080`.
Its file is deliberately absent. RC-P36-041 remains fixture-proven only,
RC-P36-037 still lacks a complete fresh Artifact set, and S3-T09 remains
blocked.

The next action is only
`S3-T09-OWNER-GRADE-V3-RESEARCH-LEAD-V3-FRESH-EXACT-ADMISSION-ISSUANCE`,
which requires separate authority. Issuance must copy the frozen payload and
digest exactly; consumption and live execution remain a further independent
decision.
