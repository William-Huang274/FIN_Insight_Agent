# 496 — FIN 0.1 S4-T06 MU R6 quarantined collect-all diagnostic and proof strategy

## Outcome

The user authorized a concentrated defect-surfacing pass before deciding whether every repair needs a separate live run. I kept formal R6 immutable and built a non-promotable diagnostic continuation around its four retained capture-v2 interactions.

The diagnostic replayed the four R6 interactions, acquired eight additional DeepSeek Pro interactions once, cached them under restricted runtime storage, completed all six logical nodes, and materialized nine quarantined Artifacts. No canonical WorkUnit/Attempt/Run or business Artifact was written. No paired assessment, owner acceptance or T07 entry occurred.

## Guardrails and verification

- Project OS: explicit `diagnostic_override` for the existing RC-P36-080 paid-execution blocker.
- Transport retries: zero.
- New-call ceiling: eight; exactly eight were acquired.
- New actual cost: USD 0.06802704, below the USD 0.10 diagnostic hard cap.
- Source runtime SQLite SHA-256 remained `203d6d0615dc1a9a00b8bda560e760454508777bf8af079c4301e43028470d20`.
- Restricted cache retained model-visible request, final assistant output and safe envelope; no credential, private reasoning or raw Provider response.
- Focused diagnostic contract tests: 6 passed.
- Adjacent temporal/audit contracts: 24 passed before execution.

## Concentrated defect inventory

Ten field-level repairs were required across four of twelve interactions:

- four R6 material-numeric narrative fields in the value-and-profit Fact segment;
- one six-to-two claim-card cardinality projection;
- three material-numeric fields in the bottleneck Fact segment;
- two percentage thresholds in the bottleneck WWC segment.

The model JSON transport itself was stable. The failures concentrated in semantic contract obedience where arbitrary narrative remained adjacent to hard truth fields.

Three project-owned cross-layer symptoms were exposed after replaying past the original failure:

1. A six-alias Numeric Fact expanded locally to 490 characters, while the shared Specialist text validator still enforced the 320-character Provider narrative limit.
2. Stable claim truncation selected a mixed-scope Fact and could not derive one local scope; selection must be validity-aware.
3. The pre-call cost guard used UTF-8 bytes as pricing tokens and stopped before Verifier even though actual cost remained within budget.

The final repaired diagnostic had correct MU identity, 12 receipts and 9 Artifacts, and its deterministic L1 surfaces passed. It is not formal proof because ten content repairs and two runtime projections were applied.

## Quality assessment

The machine Verifier marked all four layers pass and recommended internal review, but owner-grade quality was not established:

- the Chinese report exposed mechanical English numeric clauses such as `USD_millions`;
- a missing-data/gap relationship was sometimes labeled a conflict;
- the executive summary repeated an inventory-data absence claim beside exact company inventory evidence;
- the variant view was overly negative and underused disclosed company and business-unit strength;
- no deterministic paired baseline or owner review was performed.

These are L2-L4 calibration items. They remain visible but are transferred to T08-T10/S5 instead of reopening an unlimited T06 loop.

## Root-cause conclusion

The observed failures reduce to four clusters, not a long list of independent bugs:

1. material truth ownership remains too broad on the Provider side;
2. candidate generation and deterministic selection are conflated;
3. prompt/schema/validator/renderer/capacity/budget invariants are compiled separately;
4. Verifier quality coverage is stronger on L1 structure than L2-L4 investment-research usefulness.

## Proof strategy

One-by-one formal live repair is rejected.

- Deterministic selector/renderer/budget/contract-compiler changes: unit, property, mutation and DELL/MU/NVDA full-fake proof; no paid live per fix.
- Changed Provider atom families: one natural-output single-node canary per contract family, not per field.
- Failure/capture infrastructure: zero-call fault injection and cached replay.
- L2-L4 calibration: cached-output rubrics and paired golden cases first.
- Integrated acceptance: one final fresh MU exact-live, followed by independent L1, paired assessment and owner acceptance only if it succeeds.

Current next: `S4-T06-MU-DETERMINISTIC-JUDGMENT-ATOM-PLANNER-AND-COMPILED-CONTRACT-INVARIANT-HARDENING-ZERO-CALL-DISPOSITION`.
