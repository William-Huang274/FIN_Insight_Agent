# FIN 0.1.2 S4-T04 current Evidence → Agentic Research 零调用工程通过与 fresh admission

时间：2026-08-04

## 结果

T03 的 18 条 accepted candidates 已被严格分成 15 条可进入 T04 的 issuer Evidence、3 条 exact company-total numeric authority 和 3 个 typed gap。完整原始来源与候选正文仍保留在 T03 capture；T04 仅使用最长 300 字符的模型输入投影，并绑定 source snapshot、parser、locator、as-of 和 T03 terminal digest。

当前 bridge 没有修改冻结的 DELL/MU S4 合同，而是复用 FIN 0.1.2 S3 已证明的 NVDA V9 runtime。SEC 标签和 T03 搜索角色通过封闭 alias 映射编译；未知标签、未知角色、跨案、未来日期、digest drift、writer-citable numeric 与缺失 authority 都 fail closed。原 fixture 数值表面被当前 T03 数值与 capture lineage 替换，gross/operating margin 仍由本地确定性重算。

## 验证

focused integration 8 passed；fresh admission 1 passed；exact runner 2 passed；相关 T03/S3 runtime regression 20 passed。完整 fake 链达到 6 nodes、9 fake Provider captures、3 local Fact receipts、9 Artifacts；真实模型、Provider、source network 调用均为 0，业务 Artifact 晋升为 0。

fresh admission 已签发但未消费：`fin012-s4-t04-nvda-current-evidence-exact-admission-r1`，model=`deepseek-v4-pro`，预算 9 calls / 60k input / 10k output / USD 0.06 / retry 0 / source network 0。Run/Attempt=`research_run_fin01_82b97a80f3a0d79c126f7d16 / attempt_fin01_b28c8f773a80e5803dfb157e`。

## 边界

当前只是 T04 engineering pass 与 issued/unconsumed admission，不是 live 9 Artifacts、L1–L4、Owner acceptance、current NVDA R2、release 或 production。下一步只消费这一个 admission 执行一次 exact-live；首个可信失败即 terminalize，不自动重试或签发第二份 admission。
