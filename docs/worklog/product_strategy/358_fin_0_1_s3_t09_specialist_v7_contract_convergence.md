# FIN 0.1 S3-T09 Specialist-v7 contract convergence

## Authorization and boundary

The user approved the proposed contract convergence after an independent
generality audit. This tranche includes the RC-P36-044 zero-call decision,
implementation, deterministic fixtures, canonical safe telemetry, architecture
record, release contract, and Project OS synchronization.

No admission was issued or consumed. Real model, Provider, network, source,
tool, canonical Run/Artifact, business Case, comparison, Human Review, T10, S4,
release, and production counts are all zero.

## Root cause and decision

The v6 live output mixed one Graph context ref into six Fact support refs.
The strict validator correctly rejected it. The earliest owned gap was the
Provider-visible `fact_layer.support_refs` field: unlike the later Claim
`context_refs` field, it did not receive an exact field-local Evidence/Numeric
closed authority.

The selected repair does not add another cumulative version branch. It extracts
transport capabilities and reusable domain policies:

- `FactSupportAuthorityPolicy`;
- `EpistemicStatePolicy`;
- `ClaimScopeResolver`;
- `BoundedResearchProfile`;
- `SpecialistTransportContract`.

The new transport v7 declares the cumulative capabilities. Prompt generation
and local Fact validation consume the same `FactSupportAuthorityPolicy`.
Candidate and Graph remain context-only. No normalization, trimming, remapping,
deletion, fallback, or retry is allowed.

## Generality and compatibility

The NVDA company, three Cell IDs, narrative/byte capacity and owner-grade token
budgets now live in a versioned research profile instead of the admission
validator. New v7 admissions must bind the profile explicitly. Historical
admissions without the field resolve to the frozen legacy profile and omit the
field from their digest payload.

Fixtures include an AMD profile, a `2027-Q1-53W` period, and mixed
Evidence/Numeric authority. This proves the extracted policy layer does not
depend on NVDA or `FY2025-FY`; it does not claim that the current end-to-end
input compiler and product surface are already cross-company.

Transport v1-v6 refs and request behavior remain unchanged. The captured v6
failure remains failed and replayable.

## Verification

- v7 convergence contract: `13 passed`.
- canonical Fact/context/epistemic telemetry selection: `6 passed`.
- owner-grade adjacent v1-v6 regression: `74 passed`, with two known historical
  tests excluded because they assert obsolete global backlog snapshots.
- S2-T03 adjacent regression: `69 passed`, four historical live/result tests
  deselected.
- fake Provider v7 success: six logical nodes, 12 calls, nine Artifacts.
- fake Provider Graph-as-Fact negative: one call, first-segment stop, one
  restricted capture, content-free typed telemetry.
- Python compile, JSON/JSONL parse, diff check and secret scan are release
  closeout gates.

## Remaining boundary

RC-P36-044 is only zero-call contract-converged and fixture-proven. It requires
a separately governed fresh v7 proof before live conformance can be claimed.
RC-P36-038 remains open: audits against the target runtime must use direct
SQLite `mode=ro` plus object-store reads or a disposable clone.

S3-T09 remains blocked because no fresh complete owner-grade nine-Artifact
deliverable exists. The only next item is
`S3-T09-OWNER-GRADE-SPECIALIST-V7-FRESH-AGENT-PROOF-DECISION`.
