# FIN 0.1.2 S4-T05 范围入口与三案例迁移边界

日期：2026-08-04

结论：T05 范围决策通过，但当前不能直接执行 DELL 或 MU live。T01/T02 已经支持三案例；真正执行 current search 与 current-Evidence Agentic Research 的 T03/T04 代码仍是 NVDA-only，分别有 34 和 30 个 `NVDA` literal。直接复用历史 FIN 0.1 的 DELL/MU admission 或 source pack，会把历史 fixture/失败链误写成 FIN 0.1.2 当前证明。

历史资产并非无用。共同 Runtime、typed atom、本地确定性 ownership、case profile、capture-first、typed terminal、full-fake/mutation、DELL/MU 的失败 taxonomy 和 source-grounded fixture 都可以作为工程资产与回归 oracle；但旧 admission、Run、Artifact、paired 和 R2 结论全部禁止晋升。历史 DELL R10 虽生成九件套，R2 未获接受且 R11 出现新 L1；历史 MU R2 虽生成九件套，paired L1 失败，后续 replacement 也失败。

T05 固定为四段：T05-A 只做一个合并的零调用三案例 transfer package；通过后依次做 DELL current search/R2、MU current search/R2、post-transfer NVDA exact revalidation。DELL 先行，因为它是历史上最弱、最适合在继续付费前暴露共享迁移缺陷的案例。

止损规则比旧 FIN 0.1 更严格：任何新 exact-live L1 立即停止剩余 T05 paid sequence；不自动逐字段修改、不自动 replacement、不创建新产品版本。available source 的 adapter/parser/capture 缺陷仍归 T05，但只能另做一次项目级合并处置并由用户重新决定。合法检索无结果则保留 typed gap，不制造 Evidence。L2–L4 非完整性问题原则上后传 T08–T10/S5。

本轮只形成决策、projection、计划和合同测试；新增 T05 scope、既有 T04 Owner 边界和 T02 三案例 readiness 合并回归为 `22 passed in 0.95s`，JSON/JSONL 解析通过。模型、Provider、网络、source、admission、Run、Artifact、paired 和 Owner acceptance 均为 0。下一项是 `FIN-0.1.2-S4-T05-THREE-CASE-CURRENT-EVIDENCE-TRANSFER-PACKAGE-ZERO-CALL-IMPLEMENTATION`。
