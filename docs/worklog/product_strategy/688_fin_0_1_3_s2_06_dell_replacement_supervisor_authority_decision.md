# FIN 0.1.3 S2-06 DELL replacement Supervisor authority decision

日期：2026-08-07

## 决定

零调用 authority decision 通过：DELL 可获得一次 replacement R2，但当前仍不能签发或执行。理由仅限于 R1 被项目自有 Prompt/Schema/Validator 漂移无效化，且 v1.1 修复已经过双 clean archive、双 fresh process 独立证明。

## 限制

- 必须先实现 successor issuer/runner，绑定本决定、v1.1 implementation、fresh proof、clean Git 和原 DELL immutable raw/evaluation/boundary。
- R1 admission/identity 不得复用；R2 预计 8 calls、硬上限 11、USD≤0.18、retry/fallback=0。
- 本决定 provider/model/network/admission/candidate=0；不会自动执行，也不授权 MU/NVDA。
- R2 如再出现新的项目 L1/合同问题，保存 terminal 后停止，不进入 R3；L2–L4 表达问题登记后传，不进行字段式 live patch。

## 下一步

只实现 successor DELL R2 governed entrypoint 的零调用绑定，提交推送并 clean preflight。完成后仍需新的执行指令才可签发并 exact-once 调用 DeepSeek。
