{
  "schema_version": "fin_insight_project_os_full_chain_preflight_checklist_v0_2",
  "updated_at": "2026-08-15",
  "checks": [
    {
      "check_id": "project_os_context_pack_read",
      "required": true,
      "description": "current_context_pack and machine-readable ledgers must be available before expensive runs."
    },
    {
      "check_id": "root_cause_blockers_closed_or_overridden",
      "required": true,
      "description": "No open full_chain_blocker root-cause rows for the requested run_scope unless the run is explicitly diagnostic and user-approved. Scope-specific blockers can keep broad/release eval blocked while allowing a controlled single-case proof."
    },
    {
      "check_id": "token_budget_preflight_passed",
      "required": true,
      "description": "Run token-budget preflight before provider/model calls."
    },
    {
      "check_id": "provider_health_preflight_passed",
      "required": true,
      "description": "Confirm the bound provider profile, unread credential presence, and either a recent complete capture or an explicitly budgeted connectivity/auth probe. A separate probe is not mandatory when it would add a non-research paid call and recent immutable evidence is valid."
    },
    {
      "check_id": "real_evidence_mode_required_when_case_demands_it",
      "required": true,
      "description": "Cases requiring dynamic retrieval cannot be validated with dry-run evidence operators. A reviewed fixed Evidence Pack is valid only when the decision labels the run as a model-analysis unit test and forbids any Agentic Research or product claim."
    },
    {
      "check_id": "aie_and_data_script_gate_clean_for_known_blockers",
      "required": true,
      "description": "Known evidence, data, and research-quality blockers must either be outside the exact decision-bound run scope or explicitly allow that scope; a narrow allowance must not be projected to dynamic research, a five-cell case, publication, or release."
    }
  ]
}
