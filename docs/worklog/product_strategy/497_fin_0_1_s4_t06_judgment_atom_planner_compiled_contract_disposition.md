# 497 — FIN 0.1 S4-T06 judgment-atom planner and compiled-contract disposition

## Outcome

The user authorized the frozen next step. This turn completed only the zero-call structural disposition. It did not change runtime code, call DeepSeek, issue an admission, run a canary, perform paired assessment, or enter T07.

The disposition converts the quarantined R6 diagnostic findings into one bounded L1 implementation package rather than ten field patches:

1. Provider truth ownership is too wide around material numeric, date, threshold, and final narrative surfaces.
2. Candidate generation and final validity-aware selection are conflated.
3. Prompt, schema, validator, fake Provider, selector, renderer, capacity, budget units, failure descriptor, and capture index are not compiled from one typed contract.

The L2–L4 Verifier and delivery-quality gap remains visible under RC-P36-084 but is transferred to T08–T10/S5 instead of expanding T06.

## Selected bundle

The only allowed implementation bundle is:

`fin01.s4.deterministic_judgment_atom_planner_and_compiled_contract_invariants:v1`

Its three Provider contract families are `specialist_fact_atoms`, `claim_candidate_atoms`, and `what_would_change_atoms`. The Provider may return only request-local aliases and finite judgment enums. Local runtime owns validity-aware selection, ordering, cardinality, all material numeric/date/threshold rendering, case identity, canonical IDs, ClaimFactLink, lineage, and final material clauses.

The compiled policy must generate the model-visible contract, wire schema, local validator, fake Provider, selector, renderer, capacity declaration, budget estimator units, typed failure descriptor, and capture safe-index classes. Provider narrative limits must not be reused as local structured-render limits. Projected cost must use token units; actual post-call cost remains a hard cap.

## Frozen proof and anti-loop sequence

- One zero-call implementation bundle maximum; no automatic second bundle.
- Unit, property, mutation, fault-injection, R6 cached replay, and DELL/MU/NVDA full-fake proof; each case must reach `6 nodes / 12 callbacks / 12 captures / 9 Artifacts`.
- Only after independent zero-call proof: at most one natural-output single-node canary for each changed contract family, three total.
- Only after those canaries: one fresh MU formal exact-live.
- Independent L1, paired assessment, and owner acceptance are eligible only after coherent success with 12 receipts, 12 captures, and 9 Artifacts.
- A new L1 stops the sequence; there is no automatic R8 or field-by-field patch loop.
- L2–L4 findings transfer to T08–T10/S5.

## Evidence and verification

- Decision: `configs/releases/fin_ia_0_1_s4_t06_mu_deterministic_judgment_atom_planner_and_compiled_contract_invariant_hardening_zero_call_disposition_v1_0.json`
- Decision SHA-256: `d8c101e68a5f3db31b74dbe40a1ae745ee08f1ca2e50585c7dddc6065ad154a0`
- Source aggregate: `configs/releases/fin_ia_0_1_s4_t06_mu_r6_quarantined_diagnostic_aggregate_defect_and_proof_strategy_result_v1_0.json`
- Scoped Project OS preflight: `.codex_runtime/s4_t06_mu_deterministic_judgment_atom_planner_compiled_contract_disposition_preflight.json`
- Focused contract test: `5 passed`
- Broad historical `-k s4_t06` observation: `239 passed / 45 failed / 1771 deselected`. The failures are dominated by phase-snapshot assertions that require already-consumed R5/R6 identities or supervision roots to remain absent, historical exact-code hashes to remain current, and old closed `current_next` sets. They were not mass-rewritten in this disposition turn and must not be reported as a current full-suite pass.
- Model / Provider / execution-network / source calls: `0 / 0 / 0 / 0`
- Runtime code changes: `0`
- New admission / business Artifact / paired / owner / T07: `0 / 0 / 0 / 0 / 0`

## Current state and next step

Formal R6 remains immutable failed. The repaired diagnostic remains quarantined and non-promotable. T06 is still blocked, and T07 has not started.

Current next:

`S4-T06-MU-DETERMINISTIC-JUDGMENT-ATOM-PLANNER-AND-COMPILED-CONTRACT-INVARIANT-HARDENING-MINIMUM-ZERO-CALL-IMPLEMENTATION`

That implementation requires separate authority and must remain zero-call.
