# 207 · 人工完成、混合检索与跨主题记忆验收

## 展开滚动与研究方向直接编辑（2026-09-11，已部署）

- **用户反馈与纠正**：人工修改历史位于不可滚动的固定 flex 标题区，展开后被外层 overflow 裁切；前序程序设置滚动位置的验收未覆盖真实滚轮。此前仅做内部编号隐藏和自然语言投影，不能称已完成九个研究方向的用户编辑。
- **产品增量**：标题区限定视口高度并提供原生纵向滚动，历史收起入口保持可见。“研究配置”默认进入“研究方向”，九个方向均可直接编辑名称、研究问题、方法要求与边界；保存为新版本、应用到任务后，下一次运行读取。新研究、追问和局部修订的范围选择器显示保存的方向名称与问题。已有历史报告不改名、不覆写。
- **工程增量**：复用既有 native Assistant 版本、Pydantic、LangGraph 配置快照与 React/CSS；不另建配置数据库。旧配置 directions 为空时保留旧 digest；新版将方向问题投影到主 Agent 的 case catalog，按选中方向将方法要求注入研究专家。canonical 分支 ID 保持稳定；可改名称/职责，不支持任意增删拓扑或赋予工具权限。
- **验证证据**：后端定向联合31 passed，另组 Studio/BFF36 passed，末次配置与 provider 投影10 passed（有重叠，不累加）；Playwright5 passed，覆盖1440/390真实滚轮及1440/1024/390配置保存。真实HPE页面鼠标滚轮分别0→900、59→959并能收起。实际UI保存“研究方向示例 · 现金流与财年核对”，刷新回读，应用到独立未运行草稿，native下一次运行配置快照与范围选择器均回读自定义内容。该草稿0模型运行，HPE仍报告v2/人工修改2次。TS/Vite及8827文件密钥扫描通过；大bundle提示保留。
- **部署**：native18165镜像 `b0b07c765b0eda98eae4ee78e4721e246844c70b1cd106ffeae3092d68892205`，18795工作台后端已重启并加载最新前端 `index-DGpZGiRy.js`；原PG/Redis卷保留。证据在本机 `D:/temp/fin207/hpe-final/scroll-directions-acceptance.json`、`direction-native-snapshot.json`、`scroll-fixed-1440.png`、`scroll-fixed-390.png`、`directions-editor.png`。
- **文档与边界**：本段、上下文包和两项台账同步；FIN0.1.3/S3不变。本轮新增付费模型调用0。验证了配置可保存并进入真实请求构造，不声称模型必然遵循方法或金融质量因此通过。其他长期记忆、Hermes、多租户及sandbox事项不纳入本次收尾。

## HPE 人工交付收尾（2026-09-11，已部署）

- **产品增量**：HPE `01a08ca1-ecac-7913-a9d3-51ebc76a294b` 已为 `human_completed`，报告 v2，人工修改2次；两份责任底稿和4153字报告、1张12点来源绑定图表均保存。前端自动进入“已完成研究”，按财务与现金流研究员、并购与可比性研究员、报告写作显示原文/改后全文/差异。角色卡使用修后正文标题，避免把旧结论继续当当前标题。原失败、未接受候选及模型审查不覆写。
- **人工修订**：第1次修4条判断中的5处非逐字引文及全年减值归属；第2次同时改两份底稿与报告，纠正并表跨Q3/Q4、并表增量不等于有机增长、HPE净利润与普通股损益、费用总额与同比增加额、CFO/FCF/处置款/收购付款等口径，移除未证实的因果及任意后续阈值。JSON/共享引文格式恢复不算人工次数。修订依据为既有上传10-K和原计算回执，不新增网页/模型调用。
- **审查边界**：B4核验18次调用后提交；反证24次调用未完成提交。B5 Writer已经交出公开报告与图表候选，但仍有不转义引号、多余右括号及无效引用别名。报告最终自动核验未运行。经Owner明确授权，保留未完成审查，人工处理剩余问题；不是全自动金融质量通过，不能把 `human_completed` 改称全部模型核验通过。
- **B6/B7交付路径**：`01a08e56-2bab-77c1-9fec-5562d6080e46` 通过既有引用/图表门后，以新native checkpoint保存原Writer公开候选，明确记录人工修正引用绑定；旧checkpoint无报告状态保留。随后初始化到human_review，零模型。`01a08e56-c2fe-7b52-9216-a6a4213ca504` 由现有manual_complete接口保存人工正文和两份角色底稿，仍零模型。没有通过重跑整题生成最终版本。
- **工程增量**：已部署专家/Writer终态完整对象末尾一个多余 `}` 的窄格式恢复，仍走正常Pydantic、引用、图表检查；其他坏JSON只投影已解码的公开候选，不能直接变成已接受事实。生产开启已有局部字段编辑，支持根共享引文表只向已显式绑定的claim展开。修复恢复范围在provider wire漏投影、旧pending子图越权恢复、人工角色归属，以及计算摘要缺expression导致整份导出500。复用stdlib、LangGraph、ToolNode和既有版本历史，没有新建解析平台/工作流。
- **部署与验证**：前序a9957732已推送；最终native镜像 `da1a9ff445455f218e467c88121e93c5cdb9ffdebceeb8fb8f9036cdfca38c70`，API18165/BFF18795健康，review-agent容器源码SHA256与工作区一致；PG/Redis卷保留。后端阶段/恢复联合118 passed/7 skipped；末次Writer格式/source gate与候选回归43 passed/1 skipped；导出/BFF44 passed（各组重叠，不累加，skip为旧条件/私有资格依赖）。TS/Vite通过，保留既有大bundle提示；前序1440/390人工交互2 passed，最终真实HPE完成区/两角色/全文差异无浏览器错误。
- **真实文件**：由实际产品API导出MD/PDF/Word成功，PDF8页逐页渲染检查；正文表格、图表和引用可读，无裁切；Word结构检查含表格与图片。公开文本的少量Markdown强调边界与来源附录冗长属于展示余项，不计为金融或交付失败。证据仅本地 `D:/temp/fin207/hpe-final/`：导出三格式、8页PNG、界面截图和acceptance.json。源码仓库不提交私人原响应或用户上传年报。
- **研究记忆**：完成版通过既有remember接口写入按owner隔离的native Store固定checkpoint指针；真实回读确认报告v2、人工次数2、两角色修订和正文相同，零模型。这是固定版本保存/回读，不是长期偏好提炼或模型跨主题恢复通过。
- **最终费用**：本轮B1–B7共106请求/102有用量，4,380,741已知tokens，按既有公开单价估算¥21.370017，取消请求未知4；不是账单。B1=26/737381/¥3.483337/未知2；B2=10/1007926/¥6.154748/未知1；B3=10/336188/¥0.302169/未知0；B4=42/1643301/¥8.224510/未知0；B5=18/655945/¥3.205253/未知1；B6/B7均0。前两次恢复错误的¥9.638085为未推进交付的损耗，已向Owner说明。另有前轮原HPE50调用/3459160tokens/¥7.19855，不混入本轮。终态取自 `hpe-delivery-final-measurements.json`，覆盖下方停止瞬间快照。
- **仍未完成**：长审查反复读却不提交的效率问题、其他复杂题及泛化长期上下文/记忆、Hermes全研究、多租户和真实sandbox不因本次HPE交付而通过。本次授权的HPE交付及JSON局部恢复收尾；不再追加付费模型调用，不变更FIN0.1.3/S3阶段。

## HPE 交付接续（2026-09-11，历史过程）

- 费用最终回执修正：停止时的快照不是终态计费。B1 后续补齐为26请求/24返回、737,381 tokens、已知¥3.483337、未知2；B2为10请求/9返回、1,007,926 tokens、已知¥6.154748、未知1。两次错误恢复已知¥9.638085，不能沿用前文停止瞬间较低的数字。原attempt与取消记录均保留，未重置累计用量。
- B4 `01a08e31-067b-7073-bb9d-4c8d5bb0a9bc` 实际进入 counter/verifier 独立审查，未再派新研究；保留原配置、不新增重试额度。发现核验反复回读，待本次原额度终态后由授权人工处理剩余表达/格式问题，不启动同类整案重跑。人工第1次真实修改已在前端角色—底稿全文及差异中显示；报告尚未交付。
- 工程回归116 passed/7 skipped，追加同分支多角色归属检查后对应后端33 passed（有重叠，不累加）；1440/390人工历史交互2 passed，TS/Vite通过，secret scan8827文件通过。字体与有效HTML结构已部署前端；最新角色归属补丁待运行结束后加载。

- B2 普通 native resume 实际接回旧子图排队的 Q2/Q3，故再次主动停止；不能把部署新工具限制误认为旧队列已清除。新增零模型 native checkpoint 测试，证明从父 attention 节点 `update_state` 创建新 checkpoint 会改变任务 ID、隔离旧 pending 子图且保留原 checkpoint 与所有来源。实机 B3 前同样逐项验证。额外在 worker 执行前核对原 unfinished 集合，禁止旧队列绕过当前范围授权。
- B3 `01a08e29-2a3a-7eb1-a790-c3a968095fae` 不再运行新研究，但 Lead 10 次交接未通过，336,188 tokens / 0.302169 CNY，未知0。原始模型输入审计进一步查出 `_project_request` 的 allowlist 漏掉 execution/continuation policy 和本次新增 allowed_planning_tools。修复落在 provider 实际 wire 层，并扩展 MockTransport 测试直接核对 SDK messages/tools，而非仅检查内部 request。HPE 不继续付费调同一 Lead：依 Owner 授权，人工明确本题原有两项范围，使用 native attention 交独立核验/Writer，记录 `human_scope_confirmation`，不是模型自行通过。

- 接续 B1 `01a08e1f-bc08-7383-a358-c543893df8ad` 暴露新的同阶段 runtime 范围缺陷：`unfinished_only` 原实现只要求已有分支的补充任务依赖旧底稿，未禁止新分支；负责人新开 Q2/Q3。发现后主动停止，保留失败记录与 1 条取消中未知用量，不重发这两个方向。已知 16 返回 / 17 请求，410,970 tokens，估计 1.889187 CNY，不能说这笔开销达成接续目标。
- 修复为恢复时仅原 unfinished task 集合可运行；provider 工具披露同时去掉新建任务，host 也拒绝越权 delegation。原子角色底稿恢复不回滚，下一次只从 `remaining_research` 重新完成范围交接与下游，不重跑已提交研究。39 项负责人/父图回归通过，5 项旧条件跳过；旧测试原先通过重新 delegate 来模拟恢复，现改用生产已有的 `recovery_tasks` 输入验证真实路径。

- 用户明确授权代为人工修正 HPE，并要求角色—底稿—报告全文和差异、真实人工次数。沿用 S3/207，不创建新产品版本；原失败 attempt 永久保留。
- 采用 stdlib JSONDecoder 的完整对象解码、Pydantic 原校验、既有 JSON Patch 局部修订和 LangGraph interrupt。仅完整对象后一个多余右花括号可自动修复；其他破损只保留已完整解析的公共正文，不提交为已接受事实。共享引文按模型已明确引用的 evidence ID 展开，不推断金融含义。
- 工程切片：局部修订工具原来只在测试显式开启，现在接到生产 composition；完整可读候选独立于 schema 成功保存。人工交接复用同一研究暂停点、旧 notebook/source gates 与版本历史，保持原始失败和已交出的角色底稿。
- HPE 原响应离线回放：单个花括号可恢复，18 条判断；继续校验发现 5 个引文逐字匹配问题，分属 C8/C16/C17/C18。人工回读已存原文后修正，并澄清全年 Hybrid Cloud 商誉减值不等于一次测试或 Juniper 新增商誉减值。两份底稿的完整原有来源门通过；金融整体质量仍需下游审阅。
- 本次付费接续 TokenBudgetBasis：目的为已有两份 HPE 底稿的范围交接、独立核验、综合和图文报告；输入为原上传年报固定快照、已执行 40 条观察及 18 条 T1 判断、已接受 T2，不再次采集研究。必需输出为完成/未完成范围理由、保留依据的报告与至少一个图表、可见人工修改。schema 负担通过共享引文和局部字段修订降低；数字/期间/并购归因重大风险保留来源检查。对照为原 50 调用、3,459,160 tokens、7.19855 CNY 的停止记录。沿用已部署各节点 thinking/model/budget，不降低推理/输出上限来追绿；传输一次，未知请求不重试。必要审查按既有配置允许；出现模型语义争议则由授权人工处理，不反复整案付费重跑。
- 产品增量尚待真实部署和接续验收；当前工程回归 55 passed/2 skipped（原测试条件跳过），另组 BFF/投影 38 passed，与前组重叠不累加。TypeScript/Vite 通过。Project OS interactive profile 检查通过；不是金融质量或全产品通过。

2026-09-11 Owner 授权执行；FIN 0.1.3 不变。前序 206 已完成部署。207 已实施并部署，首批真实资格已有成功与失败，以下按时点保留证据；不是全产品通过。

## 单一实施顺序

1. 恢复真实服务；核对 206 部署。补齐按钮样式与根据服务端完成状态自动归档的研究工作区。
2. 角色→底稿层级接人工修改：保留原件、改后正文、版本、修改位置与次数；模型语义疑点交人处理。失败 run 不改写；新结果明确“人工修改 X 次后完成”，不得把未处理问题或引用/取数/工具故障伪装成功。复用 LangGraph checkpoint/interrupt 与既有版本化底稿；不另造工作流。
3. 资料 RAG 使用已配置 Qwen embedding/reranker 与 rank_bm25，复用标准缓存、原文定位/权限过滤；记录实际模型 ID、用量、缓存与回退。云端参数规模不作未经官方验证的断言。
4. 1–2 个旧复杂题（HPE，第二题从旧失败列表按现有资料可用性选择）各执行一次最新 runtime；简单 SQL、跨公司自由研究、普通问答在连续对话中覆盖范围更正、模型/模式切换、摘要前后精确回读。预检先于付费，使用新 attempt；遇同类模型金融语义缺陷转人工，不展开新一轮 judge 调参。
5. 完成题按 owner 归入长期记忆目录：用户偏好、研究结果/修订、原始数字回执分开保存，复用 Agent Server Store 和现有检索工具；索引指向固定版本。验证新窗口回读、过时记忆更正、跨用户拒绝和缺记录不猜测。

## 验收与费用边界

- 每个 paid authority 记录任务目的/规模/必需输出/schema/风险/对照证据/推理强度/停止行为；不为追绿反复整案重跑。无响应/未知费用不自动重发。
- 样本是开发验收，不宣称盲测准确率。比较 BM25 与混合召回的 Recall@k/MRR、引用回读正确性；长期记忆分别测偏好、公司/期间/数值/单位/新旧决定，不能只测回答成功。
- 状态分别记产品增量、工程检查、真实运行与未完成项。一次研究得到报告仍不等于所有金融语义正确。

## 已查明

- 206 部署诊断不充分：实际 Docker Desktop 位于 Z:/Docker/Docker；已恢复引擎，完成构建、部署和真实界面验收，详见 206 工作记录。用户明确要求先收尾该项，207 当前仍为规划。
- 当前工作底稿使用 Qwen+sqlite-vec，原始资料导航仍主要 BM25，二者不能混称 RAG 已全面接入。
- 图示 research_bundle_invalid_citations 属于工程校验问题，不能按模型语义疑点跳过。

## 技术依据

- LangGraph checkpoint/interrupt 用于同线程恢复，Store 用于跨线程 namespace 记忆：https://docs.langchain.com/oss/python/langgraph/persistence
- 现有 Qwen OpenAI SDK 适配器、rank_bm25、LangChain splitter、DiskCache 优先复用；价格按实际地域/模型核对：https://www.alibabacloud.com/help/en/model-studio/model-pricing

## 207 实现切片（真实验收前）

- 产品：现有报告人工审阅 interrupt 接直接编辑报告/角色底稿、原文差异和人工次数，完成后留在可多轮恢复的 human_review 节点；原始失败/审查不覆盖。自动完成区按服务端 phase 分类。文件选择器现代化。底稿画廊同时列最新人工正文和历史原件。
- 工程：人工入口校验基础版本、引用绑定、任务内底稿 ID、用户确认；数据/工具故障不能靠按钮转绿。LangGraph 原生 checkpoint/Store 持久化，不新造流程。长期记忆按 owner 保存 report checkpoint + 研究要求版本，四区域分别回读；只做目录/精确指针，不称已完成语义长期记忆。
- 原文 RAG：现有 BM25 + LangChain InMemoryVectorStore + Qwen text-embedding-v4/qwen3-rerank + DiskCache。先过滤当前文档范围。显式准备索引，搜索最多查询向量/重排各一次；未准备时明确退回 BM25，不隐式全文付费。失败/未知请求不自动重发。缓存有输入规模/用途/风险等逐调用依据。
- 已检验：共享后端 86 passed（与前次65等重叠，不累加）；人工节点6项验证原checkpoint不变、修改版本递增、无模型调用、陈旧/非法引用拒绝。前端5项浏览器检查含1440/390人工编辑和完成区；修复移动端checkbox被flex压为零。TypeScript/Vite通过，保留既有大bundle提醒。Project OS interactive preflight通过，非全产品通过。
- 尚未计入：部署、真实旧复杂题、新连续多主题问答、真实Qwen检索质量、长期跨窗口模型回读。至本记录无207付费调用。
- 开发验收材料：D:/temp/fin207/hpe-source-nodes.json（只读提取旧上传原文，396叶节点/762向量切片约111万字符）与hpe-rag-queries-a1.json（6个公开开发问题/目标页，非盲测）。预计77个批量embedding请求以及每问题至多2个检索请求；真实统计以缓存回执为准。索引写入本项目working-memory/source-rag，原年报/旧任务不修改。

## 首批真实证据与同阶段修复

- b786b963 已构建/部署，18165/18795/18806 health 200。HPE新原题attempt `01a08ca1-ecac-7913-a9d3-51ebc76a294b` 自由调度按角色模型；旧checkpoint不变。另以NVDA原引用失败题启动single/Flash `01a08cad-7a03-7091-b9ba-e46ecc3b2060`，是简单模式回归，不冒称第二复杂题。均单次，不重复追绿。
- 原文RAG真实6问：固定段落 Recall@5 BM25=0.5000、混合=0.8333；MRR@20=0.3889→0.4208；商誉目标1→5退步保留，不能声称全面更优。建索引77 embedding + 查询6 embedding/6 rerank；相同6问零API缓存回放。后续运行复用了这个索引，运行时查询费用与上述资格分开统计；不能把共享Cache累计数全归为对照本身。
- 连续对话 `01a08ca2-96eb-7ee0-94bb-5ba7582f2ead`：本部署普通对话池仅HPE/MSFT，NVDA缺覆盖被正确暴露；用户更正MSFT后批量取数/标准FCF/来源计算/底稿保存通过，第三轮普通问答无恢复旧任务。原生两次摘要已真实触发。
- 第四轮切Pro/HPE原文发生 **发送前输入上限**，不是模型金融错误：实际86674字符>80000。已有计算/底稿工具结果尚未列入可回读收起范围。局部修复复用原ClearToolUsesEdit，只在SDK实际提供read_saved_result时收起已读计算/图表/目录/底稿结果；未读批次和错误保留，specialist无该读取器时不改变其行为。read_saved_result扩展允许这些原始成功回执；不重算、不动原checkpoint。
- 离线回放保存请求：减少18011字符；同system/tools预计68663<80000；最后两条未读原文完全相同；零付费。18检查通过，补充5类回读和跨thread拒绝后15项通过（重叠）。该修复待部署，不能把离线回放当真实恢复成功。
- 同时独立检验已有安全回退：原失败窗口固定checkpoint零模型交接到 `01a08cac-c18b-7b62-8760-22637e62e605`，带1份底稿索引。真正继续答题的结果待确认。

## 首批终态与接续修复

- HPE 自由复杂题一次运行结束：50次请求均有用量，3,459,160 tokens，已知¥7.19855；T2收购可比性提交成功，T1收入/现金24模型调用后提交失败，phase=research_needs_attention，无最终报告。最后模型参数12131字符，标准JSON解码在12130报Extra data，末尾多一个右花括号；模型还把citation_quotes放在顶层而非claim内。context_digest由runtime注入，不能把原响应缺它当模型错误。严格提交拒绝是正常行为，但无效参数未投影成人可读候选仍是产品缺口。离线提取正文到D:/temp/fin207/hpe-unaccepted-candidate.md，仅供人工查看，非接受/修订/正式报告。不追加整题重跑，不改原失败。
- NVDA旧引用失败小题本次single成功交报告v1，13调用406826 tokens、¥0.280103、未知0；阶段single_agent_unreviewed，不冒称独立复核通过。两财年收入、期间、单位和来源绑定同比成功；人工措辞修改与完成确认已给Owner审阅，尚未实际确认，不能算人工完成/长期记忆真实通过。
- 第一次跨窗口真实回读：MSFT底稿/两年FCF找回，但HPE只曾读搜索预览，旧handoff既无已绑定PASSAGE也不带上传入口；模型反复寻找后触发ToolCallLimit 14/12，无最终答复。原失败保留。修复复用原TaskAttachmentStore：交接时冻结上传目录、同owner检查、原窗口只读回读，禁止读取之后新增的旧窗口文件；不复制所有历史，不把摘要当证据。
- 原ToolCallLimitMiddleware(error)会留下未配对的工具调用；采用其原生end行为，为未执行调用配对明确回执并结束当轮，后续用户可继续，未增加模型额度。27定向检查通过（handoff/agent/tools/BFF）；另9项上下文/研究记忆检查通过。此修复及e8ed4d3f已构建并部署，部署前busy=0，原PG/Redis卷保留。
- 同原连续对话第五轮新attempt `01a08cbf-2b27-7600-b5b3-35dafd52b9a9` 验证修后上下文回读：首次请求成功20806tokens/¥0.09295，再读年报后84273>80000再次发送前阻断。不称同窗口完全恢复，不提高限额。普通对话采用部署中的Pro非思考配置（与研究specialist Pro/low分开），两次摘要已用尽，不重置。
- 修后交接a2 `01a08cc0-74fd-7223-846c-b42e5d7781cd` 真正完成：7请求77338tokens（含2摘要），未知用量0，已计价¥0.282582另2个summary别名暂未计价。回读MSFT两年标准FCF、期间、美元整元与原NUMFACT；HPE收入34296、归属于HPE净利57、经营现金流2919百万美元，从原年报87/90页读取，并区分普通股股东净损益-59。无重算MSFT/恢复NVIDIA计划。新上传交接工具12次均返回，未越权。原文/数字回执后续目录漏列已补齐，按原始ID类型分区，不能让成功回读的记录在下一轮不可发现。
- 共享原文RAG缓存截至a2：143真实请求=110embedding/33rerank，分别225420/516627已报tokens，均有结果。此为索引资格+整题+跨窗口检索合计；六问对照本身89请求，二者不得混算。Qwen现有底稿语义检索不在这份原文Cache统计内。

## 本段收尾（付费停止）

- a2第二轮切Flash，2请求27887tokens，实际保存工作底稿`bc9529e6fff91c342d024f6c6b2c3a20` v1：MSFT/HPE完成项与原始索引、取消NVIDIA、下一步由用户决定；没有搜索/重算，也未推断永久偏好。
- 在busy=0时真实重启API容器；源窗口/底稿保留。新交接a3 `01a08cc9-2d8f-7122-a85d-e2d0ecaf5052` 4请求35029tokens（含1摘要）、用量未知0，正确回读原底稿和NUMFACT/PASSAGE，区分HPE57归属于HPE净收益与-59普通股股东净损益，保留取消项和用户决定下一步。工作底稿版本与数字原件分开；这是短链路持久化/恢复证据，不是用户长期偏好提炼或所有长会话通过。
- 全段97次真实DS请求（另2次发送前容量阻断不算付费请求），4,243,504已知tokens、用量未知0，已计价¥8.485815；7个摘要模型别名未计价，不能报完整账单。逐run证据在D:/temp/fin207/measured-runs.json。HPE占本段大部分费用，停止追加复杂题；旧失败不覆盖。
- 工程末次31检查通过，目录/原文回读补充后21检查通过（重叠不累加）；构建并部署4f134a64。BFF实际目录已显示交接窗口2数字/6原文；再交接窗口2数字/2原文。真实浏览器打开NVDA人工编辑，责任角色完整标题可见；重启后新窗口答案可见。截图D:/temp/fin207/nvda-manual-review.png、recovered-memory.png；旧运行中布局截图upload-modern.png仅代表其拍摄时点。第一次截图定位器使用不完整标题超时，修为实际heading后通过，不属于产品失败。
- 产品仍未通过的边界：人工完成入口目前要求已有报告，HPE这种中间提交失败尚不能只靠该入口生成正式报告；不能偷偷将其标为人工完成。NVDA措辞修改已提出，Owner确认未到，未替Owner操作confirmed，完成区/长期研究Store真实入库仍待这一步。工程合成用例通过不替代真实人审。研究Store为owner目录与固定指针，不声称跨所有研究的语义长期画像完成。Hermes本轮无新增调用，也未扩成完整研究执行器。
- 交付是产品按钮/版本/目录/RAG接线＋真实成功/失败资格，FIN0.1.3/S3不变，不宣称全图二要求或全产品上线完成。下一步需让未接受的中间候选在角色底稿层安全接受人工修订并进入后续生成；已有报告的人工修改链路等待Owner实际确认。不得再先整案付费追绿。
- 最后真实截图发现通用对话的记忆/交接/附件按钮仍为原生样式，已统一边框、圆角、字体、焦点与禁用状态；研究页之外的遗漏也收口。14项浏览器检查（390/1024/1440，含目录、交接、权限与资料批准）通过，TypeScript/Vite通过；无额外模型调用。保留原有大bundle提醒，未扩成无关打包重构。
