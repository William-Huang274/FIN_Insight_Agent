# 795 — FIN 0.1.3 manifest R4 业务审计与 clean-proof 边界

日期：2026-08-10

阶段：S1／CandidateBundle-only sparse／dense manifest

状态：R4 working-tree 零调用及业务审计通过；clean independent proof 待执行

## 1. R4 输入与结果

R4 只绑定 clean-proven R9 result／proof 与 `zero-call-r9` 私有对象。六案共物化 93 个主索引 spec：DELL 15、MU 16、NVDA 14、ORCL 19、ASML 10、ANET 19；其中已审 known-case candidate 45、留出结构化 Metric 48。ORCL／ASML／ANET 的 19 条自动叙事 claim 继续 quarantine，不进入主索引。

fake ObjectBM25 与 fake BGE-M3/Milvus 各分三批完整接收 93 个 spec；15 类 identity、bundle、source/child lineage、table path、unit、period、period role、cross-case、future date、automatic narrative、vector text 与 partial insert mutation 全部 fail closed。真实 network／Provider／model／document fetch／embedding／Milvus／rerank／Evidence 调用均为 0。result=`d84b7ef2...e7a1`。

## 2. 业务语义复核

DELL／MU／NVDA 的 45 个 target／type／slot／vector-text digest 与失败 R3 完全一致。留出三案仍为同一 48 条业务对象，期间角色=`18 instant／10 qtd／8 ytd／12 annual`；R3 发现的债券余额、期初现金、现金及应收款风险敞口、递延收入余额四项均已为 instant。R3 与 R4 的 narrative quarantine digest 相同。

这证明 R4 working tree 没有通过换对象或降低数量门过关，但不证明物理索引、召回、排序、Evidence Pack 或研报质量。

## 3. clean-proof 与下一步

先提交并推送 R4。clean proof 必须在两个 Git archive／fresh process 中只复制三份 exact capture，先重跑 R9 并逐字节匹配 committed R9 result，再物化 R4 并逐字节匹配 committed manifest result。两层 Project OS preflight 与全部调用 0 同时成立后，才允许单独做 Ubuntu WSL 真实 BGE／Milvus build authority decision；proof 本身不执行真实 build。
