# 205：聚焦判断句核验（含正确表述控制）

2026-09-10，FIN0.1.3/S3/205；接用户“聚焦检验通过后可能更改校验Agent调用方式”。先验证再决定，不直接切换生产调用。

假设：把同一份来源按具体判断句组织，并要求说明判断对象、证据支持范围与超出之处，比整篇泛化核验更可能识别含义错配；同时必须保留正确判断，不能把一切研究推断都否定。

最小切片：MSFT两条、MU三条原文目标，共三处预期需要纠正、两处合理表述控制。原句及段落限定原样保留、相关成功原生来源对象完整供应，其他来源仍可回读。私有预期在付费前冻结且不进模型上下文；目标选择有人为富集，不是盲测错误率。私有目录`D:/temp/fin205-focused-verifier-a1`。

复用现有生产Verifier、SubmittedReportReview、原生LangChain工具循环与SQLite checkpoint，仅资格脚本加可验证的目标投影。没有新调度引擎、公司规则或六skill扩写。Flash/low、6000输出上限、每题4模型/10工具及无传输重试均保持；紧凑目标结论替代重述整张数表，两题合计最多8模型调用。每次落任务专用TokenBudgetBasis。失败不自动追加调用。

通过条件：五目标均明确审查；三类语义问题均识别；两正确控制不误报；无编造来源/额外阻塞；两份完整提交。只有通过才建议在生产中小范围试点按判断/实际改动分配核验；还需验证目标覆盖与跨段一致性，不能凭五句宣布完整报告通过。

## a1失败与有依据的修正对照（执行前）

a1输入层缩小，但沿用full-report系统职责。MSFT4调用117085tokens：发现对象混用但降成advisory，添加未选全文为未决项又completion=complete，被既有schema拒绝后到模型上限。MU3次真正传输73106tokens，第四次111750输入字符在发送前被拒，未提交。初脚本误把该本地拒绝计成未知付费调用，现已依据provider_call_attempted=false修正汇总；旧result不覆盖，原事件保留。a1合计7真实调用190191tokens、新未知0、本地阻断1。

这不是聚焦语义核验的有效通过，也不能仅凭这次运行把全部问题判成模型能力。角色仍强制全文标题/监测条件检查，与用户层目标范围不一致，属于调用合同遗漏。先离线证明显式scope进入system、排除全文义务/目录扩展，再做a2新attempt：既有Verifier增加opt-in selected_claims职责，只挂精确来源/方法读取/提交工具；无新运行引擎、六方法不改、生产默认仍full_report。保留原报告校验与责任字段。

a2每题最多2模型调用、合计最多4新增调用，6000输出上限/同模型low/相同冻结原文、来源及私有预期。目的仅验证调用职责修正的效果，不续跑a1、不增加其预算、不比较完整报告覆盖等价性。真实调用前另落TokenBudgetBasis。已告知用户修正原因和有界对照，沿用已授权普通实现故障修复后的合理fresh验证范围；无自动重试。

## a2结果与人工判读

| 运行 | 真模型调用 / tokens | 结果 |
|---|---:|---|
| a1 MSFT（只缩输入，原系统职责） | 4 / 117085 | 无有效提交；对象错配被降为advisory，全文缺失被列未决，又声明complete，schema拒绝后到上限。 |
| a1 MU | 3 / 73106 | 无提交；扩大方法/来源搜索，第四次调用在发送前因111750>100000字符被拒。 |
| a2 MSFT（显式指定判断职责） | 2 / 42720 | 第一次读方法目录，第二次回读已在输入中的FCF来源，耗尽2次上限，无最终判断。预算估计过紧与无交卷预留仍是本项目问题，不能评分为模型语义答错。 |
| a2 MU | 2 / 40188 | 成功提交范围内三条判断，但内容质量不通过，见下。 |

MU的实际提交：

- T1合理控制：“CFO/净利润不独立证明盈利质量变化”，正确保留。
- T2错误推断：“利润增长伴随现金流入，而非纯账面”，被判成立。审查者增加“ΔCFO大于Δ净利润”的数值比较支持该判断，但两个合计量同步/增量排序，仍不能证明利润增长的现金支持来源或排除会计/营运资本因素；关键缺口未修复。
- T3周期反证：识别FY2024“下行底部”未经证实，却用“净利润/CFO为正”反驳谷底定位，并加入“已回稳/回升”“真正亏损低点FY2023”等本次来源未建立的结论。建议把正文改成“已转正、仍处周期相对低位”，同样需要额外依据；且允许“周期回升属主要解释”继续保留。**不能把含新推断错误的审查意见直接交作者照改**。

五条开发目标的整体验收未通过。a2只完成1/2份审查；该完整审查也未达到语义标准。没有新报告或自动修订，没有扩大生产默认调用。原方法全文仍可能触发不必要方法读取；显式scope修正不是充分条件。a2还同时改变了工具面与模型调用上限，不能单独归因某个因素或声称总体节费有效。

两次独立attempt共11真实模型调用273099已知tokens，新未知0，另1次发送前阻断；全体冻结输入不变。私有汇总`D:/temp/fin205-focused-verifier-a2/assessment.json`，a1旧错误summary保留，校正以原始event的provider_call_attempted为依据，不覆写原件。延续205口径580真实模型尝试、17686140已知tokens、历史未知2（不是580个问题）；别名价格无账单核对，不报实付费用。

## 工程结果与下一步

- 保留显式`selected_claims`作为未推广的opt-in入口；full_report仍为默认，产品调用点没有切换。它移除全文检查义务，限定已有来源/方法/提交工具，复用原生循环、原有ReportReview和精确引用检查。六skill内容不变，无公司/NLP规则。
- 聚焦目标必须是原文精确片段及原段落，来源必须是宿主观察过的原始ID，不接收预期标签；原始对象深拷贝、私有gold不进模型。
- 根据发送前阻断修复测试统计：本地拒绝不算已消费模型或未知账单。聚焦输出补宿主scope、completion与full_report_acceptance=false，避免将局部完成当全报告批准；这是实测后的离线元数据修复，不改写旧付费输出或计作新通过。
- 35 passed / 1 skipped，覆盖真实编译节点的scope注入/工具暴露/输出边界、输入原文与来源绑定、计费事件分类，以及既有报告修订路径。无UI/服务部署、无报告发布；无遗留付费后台进程。
- 工程提交`cfd31d3f`，分支`codex/fin013-conversation-and-retrieval`；本轮7个修改候选文件密钥扫描无命中、git diff --check通过。私有gold、原始响应/推理和checkpoint不提交。

产品增量：无本轮合格新报告。工程增量：试验用显式职责与范围持久化、统计修复。资格证据：语义漏检/错建议与提交稳定性仍未过关，原“聚焦通过再调整生产”的前提未满足。文档/台账保存负结果，不替代实现。

下一次应先把“证据准备”和“对已齐备证据的结构化判断”明确区分：后者必须逐目标交出判断，不能因反复选工具而不交卷；复用现有结构化模型调用，不再造通用工作流。仍需要同材料的有界模型/推理强度对照，区分语义判别能力与调用合同问题。是否改生产取决于完成率、漏检、误报、修订建议正确性和未选内容覆盖策略，不能仅看token或JSON合法。
