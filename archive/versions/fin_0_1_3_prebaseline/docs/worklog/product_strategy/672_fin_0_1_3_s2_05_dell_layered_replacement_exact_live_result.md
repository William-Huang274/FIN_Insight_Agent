# 672 — FIN 0.1.3 S2-05 DELL layered replacement exact-live result

日期：2026-08-07

状态：`exact-once complete / Experiment A DELL failed content gate / no automatic rerun`

## 结论

一份 DELL replacement admission 已在 clean/synced commit `3cb7b244...e18` 上签发并 exact-once 消费。新 successor runner 自然完成 10 个节点与 10 份 capture；transport、JSON、typed schema、identity 和完整链物化均成功。因此旧 first-failure runner 和 prompt/schema 漂移不再是当前阻塞。

结果仍不可晋升。DeepSeek 将“中个位数”利润口径写成未经 Evidence 授权的 `4–6%` 精确区间，Verifier 又以零 finding 接受。研究内容也缺少四个 Specialist 的显式反证、校准后的 WWC 路线和完整 strongest counter-thesis。隐藏 Rubric shadow 诊断为 `18/32`，不是 formal/human score。

29 条 runtime finding 已重新归类为少量共同根因，不逐字段维修：

1. 模型仍拥有过宽的自由数字/阈值表达表面；
2. Verifier 未获得或未执行足够强的金融语义比较合同；
3. 本地 classifier 的 field semantics 仍过粗，混淆事实数字、文件名数字和 hypothesis/WWC 阈值；
4. 研究规划虽覆盖六 family，但反证与行动路线没有成为必需的高质量内容。

下一步只做零调用处置：把本轮真实问题归入 S2-05 与 S2-06 supervision owner，修正 evaluator 噪声并决定 supervisor 是否应只纠正错误、还是允许补充缺失研究；在该边界冻结前不自动启动 MU/NVDA，也不重跑 DELL。
