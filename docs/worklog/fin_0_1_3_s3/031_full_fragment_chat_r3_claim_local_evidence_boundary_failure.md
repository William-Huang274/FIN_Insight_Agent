# FIN 0.1.3 S3 — full-fragment Chat R3 claim-local evidence and boundary failure

## What the natural run proved

FFJ-R3 ran against clean/synced commit `b6a65999...` with a clock-derived authority. All six DeepSeek calls completed, all three analysis drafts were visible, all three submissions contained exactly one Tool Call, and all three fragments passed their fragment-local relation, value-surface and inference checks. There were zero retries, fallbacks, external retrieval calls or protocol switches.

The model-owned business result was materially bounded:

- thesis: management says the AI-server product profitability level is in line with its stated target;
- mechanism: the current pack does not provide a reproducible product-to-segment or company profit bridge;
- counter-read: company gross margin declined on a same-basis year-over-year observation, but that company-level fact cannot be attributed to the product;
- WWC: later product/segment financial disclosure plus price, volume and mix bridge would change the judgment.

No new financial L1 violation is established by these fragments. Formal complete-Judgment L1 remains false because terminal compilation failed.

## Why the terminal failed

The Dell transcript was used as direct support for the thesis and as context for the separate mechanism. The old terminal compiler required one global role per Evidence ref and rejected that legitimate claim-local reuse as `finance_loop_micro_evidence_role_conflict`.

Zero-call continuation exposed a second defect in the same authority boundary. If the duplicate context occurrence is removed only for diagnosis, legacy Claim Authority then demands a separately marked limiting webpage. It ignores the validated `bridge_not_established` relation, the open typed bridge gaps and the same-basis NumericRelation already used as the counter-read. Adding a synthetic limiting Evidence makes the remaining pipeline pass, which proves the downstream shape but is not a valid repair and is not used to salvage R3.

## Bounded successor

The next package must be provider-neutral and structural:

1. preserve Evidence use role per narrative atom／Claim Relation;
2. derive the report-level Evidence summary deterministically without using it to authorize a fragment;
3. let validated `bridge_not_established` plus its typed gaps, and a relation-bound same-scope NumericRelation, count as legitimate boundary authority;
4. replay the three saved R3 submissions unchanged through the full current consumer and deliverable compiler;
5. retain negative tests proving global support cannot be borrowed by a fragment that omitted required support;
6. preserve R3 and use a fresh R4 identity only after two-process proof, clean push and preflight.

This remains fixed-Pack Layer One. Dynamic Research Truth Spine, five research units, heterogeneous generalization and S3 acceptance remain false.
