# 研究任务成本模型

2026-09-16 定价目标修订：本模型首先是内部的产品定位、服务分层与经营核算工具。既有请求计价和预算仍是基础；下一步先定义每档服务交付、能力证据及价格可行性，不默认建设面向用户的全部成本明细面板。用户看到约定价格、服务范围、交付条件和明确加量规则。

## 内部服务分层与定价依据

每个服务版本 k 维护：目标用户/任务、交付物、资料/公司/期间范围、研究深度、交付时效、包含的修订与更新、质量验收、超范围处理、内部运行配置及成本依据。研究复杂度和服务量可以分档，事实/引用正确性和诚实表达限制不是付费升级项。具体档位与价格待证据确认，不能将未验收复杂研究直接列为可售高价服务。

在同一服务与runtime/模型版本的可比观察组内：

`C_unit,k = (sum(C_service_task for all service attempts in k) + C_platform_human,k) / N_accepted,k`

`C_plan,k = scenario(C_unit,k, source_scale, delivery_window, cache_state, failure_and_revision_risk, sample_support)`

`P_reference,k = C_plan,k / (1 - g_k)`，其中 `0 <= g_k < 1`。

第一式计入正常服务中成功、失败、取消和返修所发生的互斥成本，以及平台实际承担的人工审核/支持成本；同一请求/重试/后代不得因同时归入attempt和task而重复加总。固定分摊沿用下文口径，只计一次。用户自己的审阅时间作为产品价值指标另记。研发诊断和刻意压力测试单列，不直接混成稳定服务单位成本；实际运行失败不能被删除。未知费用单列，存在未确定项时不能称账已完整结算；N_accepted为0时没有可报告的有限合格交付单位成本。

第二式是有依据的上线成本场景，不凭空给“风险系数”或可靠分位数；样本不足时记录假设、缺口和价格区间的条件。缓存、资料复用和价格时段的收益只有在交付条件允许且有证据时计入。完整研究未通过时先标明不可定价证据缺口，不能由局部节点费用外推成可靠整案报价。

第三式仅是基于所选完整成本口径和目标毛利 g 的内部参考售价，不是市场价值、净利润或已决定的用户报价。收费币种、税费/支付渠道及支持/退款承担按实际商业方案统一口径，避免漏算或重复分摊。g、折扣和正式价格尚未确定；最终需结合交付价值、付费意愿及可持续性选择。

上线前的交付物为“候选服务矩阵 + 单位成本/定价测算 + 能力证据与待验证项”。上线后按服务/价格/运行配置版本保存质量、实际完整成本、内部利润表现及使用反馈，持续决定调整内部实现、服务范围、时效或新订单价格；已经承诺的服务不静默缩水，也不自动追加收费。

用户侧与内部执行分离：价格/包含服务/限制/时效/加量条件对用户明确；token、Agent调用、返修拆分、缓存假设及利润测算默认内部使用。内部预算控制用于履行约定与防止失控，不能转成固定请求次数强迫研究结束，预算不足也不能默默删掉必要核查并交付。现有费用记录保留，BYOK等产品是否展示供应商用量由其服务定义决定。

2026-09-14 · 成本设计 v0.1。固定价格和显式场景预留已通过可选 PostgreSQL 预算执行接入研究入口；自动成本预测和校准尚未实现。复用既有用量记录、`usage_pricing.py` 和供应商账单，不建设新实验追踪或计费平台。

预算接入已有真实流式请求和保存后回放的局部验证；这些结果不能代表完整研究任务的成本或供应商账单。后续按下述口径记录完整案例的所有成功、失败和未知用量。

## 实际用量计价与总服务成本

对请求i，以非重叠的未缓存输入U、缓存输入H、计费输出O计价：

`C_model_i = (U_i * p_u_i + H_i * p_h_i + O_i * p_o_i) / 1_000_000 + C_other_i`

价格单位为该币种每百万tokens；倍率、长上下文阶梯、分时价格等进入该请求适用单价，并记录价格版本/时间。推理tokens若已包含计费输出不再相加。图像/音频等非token项用供应商对应计费单位计入C_other。供应商input字段若含cache_hit，U=input-H；字段定义未知时不强行换算。

`C_variable_task = sum(all attempts and descendants C_model_i) + C_embedding + C_rerank + C_search/data/OCR + C_compute/storage/egress`

各项互斥归类，embedding/rerank若已在请求总账中不能再次加总。失败/取消中已发生支出保留，未知支出单列，不能计为0。多币种先分别汇总，需要统一币种时记录汇率及日期。实际账单、按历史公开价估计和优惠/赠送额度分别保存。

`C_service_task = C_variable_task + allocated_fixed_cost`

固定连接器/数据订阅/runtime许可/服务器待机等按明确的月度使用或用户分摊策略一次计入，不能既按请求收费又完整分摊同一费用。用户BYOK自付费用与平台承担费用分列。用户审阅/修改时间单独记录，不假装平台已支付这部分成本。

同类任务的`C_quality = sum(all attempted task costs) / accepted_deliveries`；记录是否人工完成。分母为0时不报告有限成本。该指标不能代替质量、时间和介入率本身，也不能跨不同任务/报告标准混算。

## 预测和执行中预算

`C_forecast = sum_stage N_stage * E[cost_per_call | model, input_scale, corpus_state, workflow_version] + expected_nonmodel_cost + fixed_allocation`

初期N、输入/输出/命中率为有依据的任务场景参数，保持普通查询、首次研究、资料增量更新和用户修订分组。不能把研究结束后才知道的调用数用于声称事前预测准确。

`C_expected_total_at_t = C_known_so_far + E[C_inflight_unsettled] + E[C_remaining_work]`

未结算请求与已知费用不重叠；后续仍必需的综合、核验和交付计入剩余工作。调度入场使用保守资源预留而非均值预测，多worker共享任务/用户预算；已完成后以实际计价结算释放余量。价格预估、供应商在途请求和未知用量意味着事后费用仍需核对，不能声称软件能让外部已经发生的费用消失。

## 校准规则

每个完成或失败任务保留执行前预测、预测版本、实际用量、价格版本、质量结果与人工耗时。按任务类型和runtime/模型版本比较预测偏差、绝对误差及区间覆盖率；达到样本要求后用最近同类完成记录更新阶段调用数、长度、命中率和升级/失败频率。价格变化更新单价表，行为变化更新预测参数，二者分开。

样本少时显示保守场景和样本数，不伪造P50/P90。定期回放已保存的事前预测；不得覆盖旧预测或丢弃贵的失败任务。校准模型先用分组统计，只有预测误差证明必要时才引入更复杂方法。自动化校准未实现，本文件定义的是下一步接入要求。
