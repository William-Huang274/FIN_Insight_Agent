# 017 · E3 方法实际消费与分部边界

2026-09-14；对应 PRD P1、执行路线 E3。状态：方法入口实现与实际消费通过；数值核算局部接受，披露边界/归因质量未接受，不发布 0.1.4。

## 问题与最小修正

013 原模型五轮只读资料/计算/提交，没有方法读取；后续分部表在解析结果中可用，原模型未读到该块。因此不能仅把错误归为公开资料缺失，也不能先建设新的解析平台。同步专家没有其他原生角色已有的 METHOD_TOOL_GUIDANCE；非原生结构化入口还会发送前重建提示、覆盖追加指引。本包复用现有方法工具、MCP、LangGraph 和 SDK，修正两个入口，不引入方法引擎或新增方法 ID。

finance 方法补充分部/产品/跨分部指标的包含关系、期间重列、同口径组成数计算、合并调节以及负面披露结论前的相关表格覆盖检查。依据是 IFRS 8 官方概览对分部及产品/服务相关披露的区分，以及来源定义优先原则；不把 IFRS 强加给 US GAAP 发行人，不在方法中写 MSFT 答案。目录仍六项，已保存 Studio 配置不被改写。

## 预先固定的资格边界

假设：方法必须先进入实际请求才有机会改善金融判断。先用真实 SDK MockTransport + 真实专家图/MCP验证无正文、直接提供全文、目录选择后读取三条路径；方法不能变成案例证据。该工程检查不是模型自主消费或质量证明。

随后最多一次三臂 DeepSeek Pro 非思考开发对照：unavailable 返回明确无方法正文，direct 在初始请求绑定 finance 全文，dynamic 使用目录和读取工具。三臂共享系统指引、同一原始问题、固定来源、模型、输出4000、每臂最多10请求/20工具动作和180000 UTF8 bytes；无正文臂仍有通用系统约束和原工具，不能称为完全无方法系统。每臂独立任务、资料快照、PG预算8元/交付预留0.1元；估算用当日18点后的官方空闲价，时段改变则停止。根预算并非预期花费；012/013单臂约0.41元高峰价为历史参照。

TokenBudgetBasis 固定在 test_method_comparison_live.method_configuration：历史5轮 + 目录/方法2轮 + 补表/计算/修复3轮；原最大113596 bytes，放宽至180000容纳全文方法和新增工具历史。质量/必要工作优先，不靠省略核算缩预算。每个调用持久保存输入、响应、usage和预算；未知不重发，截断/预算/工具边界失败原样保留，使用 pytest --maxfail=1 防止失败后继续付费批次。

判据：审阅全部正文、claims、反证和 gaps，核对季度/全年、单位、利润率及精确引用；分部与产品身份、跨分部指标、无证归因、未读表格被称未披露均为实质错误。仅公司层面问题不强制展开所有分部，但不能附带无证分部缺口。不是盲测，不读取隐藏评测答案；每臂一次不能证明统计显著或普遍因果。若直接提供仍无改善，先修方法/工具/模型；动态单独失败才修选择，不扩建平台。

## 本地证据

- 初轮无正文夹具漏回 method_id，触发现有身份校验；失败保留于 pytest-1811，未触发外网模型。
- a2 定向失败1/通过61：旧结构化入口覆盖提示，已修最早入口，失败保留 `.local/fin014/20260914_e3_method_wiring_a2/pytest.txt`。
- a3 定向62通过，10.64秒，包括三臂真实SDK/专家图/MCP、本地配置及方法测试。动态目录和方法全文均逐字到达后续实际请求；直接全文到达首请求；零模型费用。

## 真实对照与后续

attempt `.local/fin014/20260914_e3_method_comparison_a1`；工程 pytest 3通过、237.69秒。每臂都有独立 product、paid_preflight、private_model_audit、specialist_result、source_readback、final_budget；代码清单与 implementation.patch 保存在根目录。全部20请求、574222 tokens（554768输入/19454输出），公开价PG逐请求取整估算0.932234元，held/unsettled均0；不是供应商账单。隔离Docker服务结束后全部停止，原卷与失败记录保留。

| 臂 | 请求 / tokens | 估算元 | 方法实际行为 | 语义审阅 |
| --- | --- | --- | --- | --- |
| unavailable | 8 / 235576 | 0.474785 | 请求目录，收到明确无正文 | 季度/全年、单位、三项来源计算正确；正文将利润率改善进一步解释为经营杠杆或成本结构贡献，后又说具体原因无法判断；具体驱动“公告未分解”也未完成相关表覆盖。局部接受，不接受全稿质量。 |
| direct | 6 / 177763 | 0.248535 | 初始全文在请求中，仍重复读目录和finance全文 | 正文、C11、反证和gaps反复断言公告没有分部营业利润；同源表有，明确失败。方法送达不能解释为方法被正确执行。 |
| dynamic | 6 / 160883 | 0.208914 | 先目录后finance全文，进入后续请求 | 核算正确，部分措辞改为“本次读取范围内”；但正文仍以“公告仅给出分部收入”起句，继续将分部利润当作有待外部取得的数据，未补查同源表。与直接臂相比边界措辞较好，仍不足以关闭013负例。 |

三组仅实际读取正文块1:0、1:1、1:2；分部表在1:4。宿主从该臂真实任务副本回读并保存 `direct/host_segment_source_readback.json`：季度分部收入为33112/29878/13451，上期28627/23785/12315；营业利润为18993/12140/3190，上期15706/9835/2384，百万美元，表头同时给出季度/全年。该证据可直接反驳“未披露分部营业利润”，不能把尚未点击的表格归为商业数据缺口。源原件仍为既定官方公告，不读取隐藏评测答案。

unavailable与dynamic各有一次已知无效JSON工具输出，原响应保存，宿主反馈后模型在剩余预算内修正；不是网络重发、截断接受或未知重试。direct的最大载荷147052 bytes、unavailable155115、dynamic126670，预先放宽到180000确实容纳了方法与修复历史。direct仍重复读取已经给出的全文，属于后续效率问题，不据单次费用排序声称质量/成本优胜。

完整默认回归1511通过/161跳过，103.77秒；保存于 `.local/fin014/20260914_e3_method_regression_a1/pytest.txt`。资格辅助函数复用原单源runner，默认仍六请求/十二工具动作；E3显式传入十/二十。本包无前端修改。

决定：接入修正保留，finance补充作为待验证方法维护，不能升为已证有效方法。停止对该题付费改提示；下一小包先用既有解析/导航组件验证表格定位信息在目录中的可发现性，并围绕已证明可读却未查的资料做确定性补查/缺口反馈切片。直接全文仍失败，故不把问题误归为动态选择器、不新建方法平台；后续改动需新attempt和迁移问题才能主张改进。E1/E2开放项继续保留，E4/E5未由本包实现。
