# 205 HPE 原候选局部修复 TokenBudgetBasis

2026-09-09，FIN 0.1.3 / S3，延续 Owner 已批准的复杂题根因修复。不是新产品版本、不是 HPE a3 全链路运行。

- 节点目的：让原 DeepSeek Flash 依据原候选与实际观察修复 SubmitWorkpaperAction。原 Q4 第24轮有两条 numeric_fact / not_applicable 冲突，不能由后台自动改标。
- 输入规模：原 progress 817,620 字符、35观察。按原候选引用与计算操作数来源选择完整原始条目，去除完全相同的观察副本；不摘要原文，不携带私有推理。原候选9,899字符、原任务2,298字符。调用前 prepare-only 写入实际输入并测量；实际字符数记入 request.json，不能冒充 tokens。
- 必要输出：一份完整原生 SubmitWorkpaperAction，保持原6条 claim 标识、计算与限制，按证据纠正分类/引用/精确引文。不能靠删除困难判断取得通过。
- Schema负担：只提供既有提交工具，无工具执行。host context_digest 使用明确标注的诊断绑定，不伪造原 checkpoint 绑定。
- 质量风险：格式通过不保证金融语义正确；使用现有 _submission_errors 检查全部原观察中的来源、权威等级、精确引文、计算回执及冲突。归档语义投影缺完整 artifact_digest，因此不宣称完整 notebook/receipt 验证或正式入库。
- 对照证据：HPE a2 Q4原始最后提交失败；全题 a2 已51调用、2,140,450 tokens。此次只允许1次诊断调用，无自动重试、无整题重跑，不计作题目成功。
- reasoning profile：deepseek-v4-flash，thinking enabled / effort low。max_output_tokens 16,000，为完整候选/schema和推理预留空间，不作为必用额度。
- 停止：SDK timeout480秒，transport max_retries=0；截断、传输/校验失败均保留输出与用量，不解析截断结果为成功，不隐式追加调用。后续必须基于具体新证据决策。
- 成熟栈：沿用已有 compare_review_model_once 资格脚本、ChatDeepSeek SDK、原生 Pydantic schema 和 LangSmith。没有增加执行引擎或研究状态机。

产品验收仍要求新运行的端到端报告、失败可见性、引用与财务语义检查。局部诊断不能替代这些检查。

## a1结果与a2依据

a1真实1调用，12,151输入＋11,358输出＝23,509 tokens，87.447秒。Schema与已有引用检查通过，但模型把6条claim改成5条，退回完成日期/无形资产寿命等必要内容，因此`next_action_invalid`，原件保留在D:/temp/fin205-hpe-submission-repair-a1。不能登记为整题或局部修复通过。

新增只读证据：原35观察实际包含67个不同PASSAGE标识。HPE原候选多条不同断言只引用同一购买价表中的短语；短语匹配不等于支持整条断言。已读PDF物理134页有完成日期，135页有融资方式/Networking归属/替代股权奖励，136页有寿命/摊销和测量期调整。它们已在原观察中，不是外部信息缺口。

a2只允许新增1次诊断：把上述原观察的4个精确PASSAGE标识作为显式补充输入，入口验证均属于原attempt；保留原全文、引用标识和所有6个claim_id，纠正无增发即无稀释的过强推断。沿用同模型/输出上限/timeout/无自动重试。它是有新证据的输入修正，不覆盖a1、不扩到整题重跑；a2后无论结果均先检查再作后续决定。

## a2结果与下一项工程

a2输入48,697字符（逐原始条目去重，未摘要）；真实调用18,373输入＋16,000输出＝34,373 tokens，117.199秒。输出达到上限，finish_reason=length，0完整工具调用，原始响应保留，未将截断内容提升为成功。两次合计57,882 tokens、按既有2026-09-07价格场景估算¥0.2382345，不是账单。

停止追加付费调用；先完成局部修订工程。采用环境已有jsonpatch 1.33（补直接依赖、uv lock），用标准RFC 6901指针和RFC 6902 test/replace，FIN只限定可改的底稿字段、准确基线及claim身份，修改后运行原Schema/来源/计算校验。ReviseWorkpaperAction只在已有被拒底稿时允许，不能修改context绑定或接受报告、不能删掉困难claim来过关。初次提交与广泛重写仍使用原工具。

此新增工具的下一次付费资格仍需按具体待修字段/原文规模测量输出Schema负担；本次只做零模型SDK/ToolNode确定性资格，不凭“输出更短”推断财务准确率或整题节费。

## Owner继续授权：完成第一步真实闭环

Owner在已知前述失败和部署审查阻塞后明确要求“继续，直接把第一步全部跑完”。本次重新执行既有服务构建/重载，保留PG/Redis与历史，不涉及sandbox配置/宿主工具扩权。原生profile预检已通过，0模型。当前scope为局部修订/既有证据回读→HPE新全题→产物和失败显示检查。

局部a3资格使用新ReviseWorkpaperAction，原候选6条判断与a2同一组原始资料；只允许字段修改，标准JSON Patch原子应用后再跑完整校验。不恢复旧run、不将归档投影伪造为完整原生checkpoint。thinking disabled用于精确格式/引用修订，避免重写整篇；Flash、12,000输出上限、480秒、1次transport/无自动重试。实际输入含提交Schema，调用前prepare-only记录字符数。必要输出是分类/支持引用和过强表述的修正，未更改字段原样保留；不得删claim或降低证据要求。前两次完整重写失败为对照，a3若仍失败必须保留原因，不自动重发。

a3仍截断：18,953输入＋12,001输出＝30,954 tokens，90.030秒；公共content约41,004字符，工具参数未完成。原生修订工具尚未获得真实资格。定位到诊断入口仍使用tool_choice=auto，允许长篇自由文本先于修订消耗输出。查询DeepSeek官方Chat Completions文档（https://api-docs.deepseek.com/api/create-chat-completion/ ）确认支持指定函数；a4只做一次同输入、同12,000上限、disabled thinking的named ReviseWorkpaperAction诊断。使用SDK参数，不新建格式恢复器、不解析截断JSON、不替模型改答案；保存所有失败。此实验只检验局部已知修订，不能把固定工具强加给自由研究调度。停止条件沿用前述，异常不自动重试。

a4输出结束于tool_calls，content=0，18,960输入＋4,755输出＝23,715tokens，36.822秒；但参数有未转义引号，SDK归类invalid_tool_calls，未执行补丁。已知局部任务也不能因工具参数错误直接判为模型能力不支持整个研究。a5同一已知输入用Pro（disabled thinking、指定同一工具、12,000输出/480秒），只做1次能力对照；不升级整题默认、不自动降级/重发。必要输出、6条claim保留和引用语义检查均不变。若仍失败，停止该单调用资格路线，回到原生工具错误反馈闭环或上游工作纸契约，不再连续换提示词付费。

a5收到1个完整JSON工具调用，18,960输入＋3,298输出＝22,258tokens，31.502秒；但把/claims数组的claim_id当作索引，JSON Patch拒绝，不执行、不自动猜测对应。停止无反馈的单调用重建路线。工程上补齐RFC6901数组索引说明，诊断输入补入原candidate的真实Pydantic错误（之前诊断只有文字分类提示，未与生产错误反馈保持一致）。a6改为原生AIMessage/ToolMessage接续a5已拒工具调用，给出标准错误反馈、保留原候选与基线；仅1次Pro disabled/12k/480秒、无自动重试。目的为真实检验模型读反馈后纠错，仍不是完整notebook入库或整题通过。a3-a5累计76,927tokens，均失败原件保留。

Owner提示9月9日可试DS V4.1 Flash，已用当前官方账户GET /models只读核对，返回v4-flash/v4-pro/v4-flash-vision-exp三个标识；官方文档尚未查到v4.1独立名称。未猜测别名、未宣称测试新版；不阻止现有第一步继续。

a6接续输出299 tokens，但提出修改/context_digest，原子权限校验拒绝，22,484输入＋299输出＝22,783tokens，5.918秒。字段修订真实资格未通过，停止追加其付费资格。默认编译路径关闭字段修订，隔离SDK资格显式启用；完整候选仍保存并回传，正式研究沿用已有SubmitWorkpaperAction与原生ToolMessage错误反馈（SDK模拟覆盖schema→引用反馈→完整提交）。没有降低引用/计算/审阅要求。诊断另发现与生产host绑定不一致，已改用相同_native_function_schema与_bind_native_call_context，不覆盖模型自行提出的不同context_digest；此项仅零模型检查，不能洗掉此前失败。

HPE全题a3为同S3根因修复的新诊断attempt，不是字段补丁资格或发布通过：保留原题/原176页官方10-K/auto+Flash/既有各节点预算、16k keep2原文回读与禁重复失败任务规则，采用已部署的新数字事实分类说明、失败候选持久化和已有完整提交路径。必要专题与报告要求不删；完整运行只能1次，无自动重试、无改旧失败、无升级默认模型。当前费用对照为a2的51调用/2,140,450tokens、最大单次79,271输入tokens。目标是检验新原生路径是否形成受证据支持的完整产物；若仍失败，保存实际阶段/完整已完成结果并停止本次整题，不另开替代任务重置额度。字段编辑质量阻塞保持开放，但不让未通过的实验工具成为本次候选的依赖。

## a3结果及具体上下文根因修复

全题a3停止于research_needs_attention，原生success仅代表控制流程完成；49实际模型调用、1,918,418tokens，峰时场景估算¥2.4099352，未生成报告。Q1/Q2候选正文6,405/4,342字符与反馈已真实保存在原生checkpoint；Q1明确请求人工接续，说明财报PASSAGE刚读取即被省略。原候选与失败保留，不重发原任务。

已从原始SDK请求复现工程缺陷：ClearToolUsesEdit的keep=2按工具数量保留，平行批量返回3–4个结果时会提前清掉本轮尚未交给模型的前几个。a3中18个请求共21个新结果在首次交付前被清除。薄适配修复为保留最后AIMessage之后所有未消费ToolMessage；仍用原生编辑器清旧历史、保持工具配对和错误；不扩大硬输入上限，不做摘要或创造引用。离线回放49真实请求/113新工具结果，修后首次丢失0；六项上下文检查包含4结果/keep2和消费后可清理，0模型。

据此准许同一S3新的a4全题诊断1次（非自动重试）：同题/原资料/auto+Flash/全部既有角色预算与金融质量要求；唯一本次新核心修复为新工具批次首次完整交付。字段补丁继续默认关闭。不得因达到输入/轮次上限擅自删研究，失败必须保持实物/原因；完成后检查新结果首投递、来源引用与产物，而不是仅看任务status。原始a3归档不可变。官方V4.1独立标识尚未确认，暂不把旧别名的结果当新版对照。

## a4结果与排版引用修复后的同窗口接续

a4实际48调用/2,465,574tokens/峰时场景估算¥3.4982508，无未知；Q1底稿submitted，Q3增长拆解needs_attention，其余未执行，无完整报告。累计416实际尝试/13,704,212已知tokens/估算¥12.5461622/未知1。保持原attempt不可变。

原Q3末次9处引用错误，8处只是PDF换行/表格空白/百分号前空格，1处把不连续句子拼接为单一引文。FIN引用校验新增薄的标准库词/标点序列比较：仅容忍排版空白，数字/词边界、标点、大小写、单位与顺序不变；专家提交与责任作者修订共用。原失败候选不改，离线校验9→1，真实不连续引文仍拒绝。77定向检查通过、1材料依赖跳过，0付费。

下一实际资格使用已有同窗口continue_remaining入口，保留a4已提交Q1和历史失败，以新run只推进未完成研究，不创建替代整题重置已完成工作。沿用原题/材料/必要输出/各角色TokenBudgetBasis、Flash/auto配置、模型/工具上限与停止条件；新增根因仅排版引用适配，字段补丁HOLD，不能修改原候选替模型提交。原同尺度a4为费用依据。新run最多一次，执行失败不自动重发；每条已提交研究仍须后续实质审查、报告及多格式验证。原生profile预检与部署完成后才执行。

接续a5实际49调用，48次用量已知969171tokens/估算¥1.4935874，1次兄弟取消导致用量未知。已停止付费，不重发该请求；源码限额退出修复不扩大任何原预算。第一步财务质量未通过，后续不应再使用从头替代任务来重置额度或把未完成降格成完成。

## 三问题修复后的最小局部核验（Owner本轮授权，非全题重跑）

先零模型验证：主问题覆盖及底稿绑定、范围交接传入独立审查、逐条发现保存/撤回说明、限额前交接提醒、来源窗口按需读取、失败公开投影。原HPE/a5不改，未完成问题仍开放。沿用LangGraph/create_agent/MCP/现有计算器，不新增调度器或压缩引擎。

通过后最多一次单Verifier局部资格：原a4已提交底稿及原归档来源，核验收购费用/减值对利润变化的解释。只发送公开披露及模型原底稿，不注入人工纠错答案，不是盲评；不启动Lead、其他研究员、Writer或Hermes。Flash thinking enabled/low，最多6模型调用、16工具、每调用100000输入字符/6000输出tokens/180秒，transport=1、重试=0；达到上限保存实际发现与未决项，不能当完成或自动续跑。成本根据真实usage记录，不与a5整轮作直接节费百分比比较。

TokenBudgetBasis由已有Pydantic验证并随manifest保存：旧NodeRole枚举用counter表示审查类预算，实际actor明确verifier:archived-focused-review；必要输出为现有CaseReview的判断定位、来源核验、修订意见和未决项。原49调用/969171已知tokens/1未知为失败规模背景，不是同条件基线。执行入口qualify_archived_case_review.py复用生产reviewer和官方MCP/SDK，原生InMemorySaver仅用于隔离资格；生产持久化仍由Agent Server负责。prepare-a1角色枚举错误0模型，prepare-a2通过0模型，均不删除。

## Owner继续授权：验证关闭读工具后的真实提交

局部a1已用6调用/110284 tokens/场景估算¥0.3875872，未提交发现；不能视为金融质量通过。根因为倒计时提示没有阻止末两轮继续读取。随后已完成原生middleware工具过滤：在已读完整底稿且仅剩两轮时只保留保存发现/提交审查，原始历史不变；144通过/7材料依赖跳过，另14项审查检查通过。零模型回放证明末两轮请求工具集合改变，不证明模型能提交。

Owner现明确“继续往下做”。以新a2做一次同题、同原底稿、同原来源和同6调用/16工具预算的对照，仅变化是已经通过本地检查的末两轮工具过滤；不整题重跑、不改原失败。若形成可用来源绑定发现，再按既有责任作者修订路径单独制定预算。若仍无可用提交，先定位具体错误，不再次照原条件付费。此资格不覆盖整题自由范围的充分性或全部财务质量。

a2保存1条费用总额/同比增量混用发现，未完成审查提交；模型仍尝试已关闭的读取工具并被拒绝，另1条发现将不连续底稿文字拼接为引文而被拒。原错误保留，不人工补写模型发现。只将已保存公开发现交给已有build_case_output_agent(role=repair)进行一次责任作者资格；明确原审查未完成、建议中的因果也可能错误，不注入审查私有历史。原纸仍不改。

责任作者a1：已有一份约数千字底稿、1条带原文数字行的发现及来源目录；6调用/16工具/100000输入字符/10000输出tokens/240秒，Flash thinking enabled/low、transport1/retry0。输出上限高于审查的6000，是因既有PaperRevision须返回改变的claim、完整替换叙述和逐条回应，不能以费用压低造成必然截断。只改所列问题与相应叙述，其他claim保留；不生成全报告、不启动其余专家。prepare-only已通过0模型。此前单次JSON Patch路线未通过且继续HOLD，本次直接验证既有原生工具反馈循环。任何运行/语义失败都不能当闭环完成；完成后才安排独立复核。

责任作者a1首调用29682tokens，成功读取4个来源；第二请求104295字符，超过100000输入上限，在transport前停止（0第二请求费用）。定位host seed的sources目录单独45715字符，原底稿13290字符，明显重复占用。生产paper_revision_input去掉预塞完整来源目录，保留完整原底稿/发现/来源ID和现有按需读取工具；没有删首次未读工具结果或私有reasoning。29项收敛检查通过、1私有材料依赖跳过。

责任作者a2为同actor原始消息的隔离接续：只把第一条host seed换成生产薄投影，其后所有原始AI/ToolMessages（含4个已成功来源结果）原样保留；原a1与原HPE不修改。余量严格扣除已用1调用/4工具，最多再5调用/12工具，仍100k输入/10k输出/240秒/transport1/retry0；不是新窗口重置6调用，也不重复第一次查询。manifest固定原state/feedback/续接state的SHA与问题；prepare-only通过0模型。

作者a2实际2调用：首请求55983字符，成功完成绑定计算；第二请求82243字符，输入24685/output9998，其中reasoning4119，PaperRevision参数约10552字符尚在第二个更新claim处截断。源证据已读，瓶颈转为完整修订输出不足；不是API不可达或引用校验不通过。禁止修复/执行截断JSON。

作者a3只校准该完整修订的输出到16000（包含thinking），不增加调用总量：原a1的1次+a2的2次实际transport均计入，总6次中只剩3次/11工具；包括未进入graph state的截断调用。native保存的模型/工具消息原样接续，原截断响应在private audit完整保留，不喂入不可配对的截断调用或人为完成候选。输入仍100000/240秒/transport1/retry0，不改全局默认或审查预算。prepare-only证实剩余3次。仅此一次校准；如仍无法提交，不再提高输出上限或重置预算。

作者a3用1调用正式submit_paper_revision成功（24685输入/10242输出，含4647reasoning），基线原件不改。修正费用增量/桥接并回读注记修订减值归属；当前仍保留43M差额等open_gaps，不等同语义PASS。整个责任作者链共4次实际调用（含截断），未用满6次；不再续作者。下一局部独立复核1次：使用同原底稿+已验证提交的revision公开视图/原来源/新计算绑定，不传作者私有历史，检查同一营业利润桥接与减值归属及叙述一致性。预算沿局部reviewer6调用/16工具/100k输入/6000输出/180秒/无重试，不启动整题、Lead或Writer；无论结果均保存，不循环追跑。

复核a1第3个实际调用截断：output6000中reasoning5594，只有406留给公开工具结果，不能以此判金融失败或完成。这是隔离资格输出预算不合适，正式runtime的repair输出原本32000，本轮未下调/修改。将该复核输出单独校准一次到12000，原六调用已用三次（截断计入），续接a2最多余下3次/10工具；不重读已经保存的来源、不加入人工作答、不再增加调用总量。保留全部失败，超过余量或再次截断后停止付费，不循环整题。初始reviewer seed和所有保存的AI/ToolMessages原样保留。

## Owner批准本地回归后的一次明确验收资格

Owner选择“先把失败响应做本地回归，再一次真实检查模型质量”，本轮不接续旧耗尽预算。根因是旧局部入口仍暴露全篇读取/全篇审查语义；新增revision-only review target由原纸和正式revision机械比较生成（标准difflib、现有canonical digest），含实际改变的claim及正文上下文差异，不含人工财务答案。原生create_agent只暴露目标读取、原来源ID读取/计算与现有提交；其提交固定baseline/current digest与revision_only标识，不能成为whole-case PASS。越界发现被拒且可明确提出必要范围扩展。普通完整研究审查保持原语义。

零模型100检查通过2私有材料依赖跳过；同批有效发现/字符串坏参数/拼接引文/越界claim和完整父图不能提升局部结论已覆盖。新一次资格D:/temp/fin205-hpe-scoped-review-a1只评原已交C_GAAP_OP及机械正文改动，原来源/原计算仍可按需回读；不启动作者/Lead/Writer，不改旧thread，不试Hermes。Flash thinking enabled/low，6调用/16工具/100000输入字符/12000输出tokens/180秒/transport1/retry0。目标约17263字符、1改变claim/7正文差异窗口，manifest保存任务专属TokenBudgetBasis。必要输出是明确局部判断、正确引用的发现或确切未决扩展；正式提交≠财务无错误，更不等于整题验收。完成/失败均保留，不再加预算或自动续接。

实际a1在transport前因严格tuple与JSON列表形态的预算预检失败，0模型；修为原生model_validate_json后prepare-a2通过。唯一付费为scoped-review-a2：6调用126018tokens/估算¥0.2509436/未知0，形成局部提交但减值归属未核实完整，不能金融验收。模型漏填未决列表而旧schema默认[]；最终schema增加显式completion及未决列表，真实原响应原生回放被拒、原件不改，最终101检查通过2跳过。预算已停止，不为最终schema补丁追加真实调用；工程通过与生成质量仍分开记录。详205工作日志。
